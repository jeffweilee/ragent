# 00_plan.md — Master TDD Implementation Checklist

> Source: `docs/00_spec.md` · Authored: 2026-05-03 · Reorg: 2026-05-04 (Round 3)
> Workflow: `CLAUDE.md` §THE TDD WORKFLOW · Tidy First (`[STRUCTURAL]` / `[BEHAVIORAL]`)
> Each `[ ]` = one Red→Green→Refactor cycle, each cycle = one (or two) commit.
> Reorg driver: `docs/team/2026_05_04_phase1_round3_reorg_auth_off.md` (12/12 6-of-6).

## Status legend
- `[x]` delivered
- `[ ]` TODO
- `[~]` scaffolded-but-disabled (P1 OPEN mode — auth track deferred to P2)

---

## Phase 1 — Tracks (organized by domain)

> Tasks are grouped by domain track. The `Week` column preserves the original 5–7-week schedule for PM. The `Depends On` column lists prior task IDs that must be `[x]` first.

### Track T0 — Foundations (utilities & state machine)

| # | Category | Task | Depends On | Status | Owner | Week |
|---|---|---|---|:---:|---|:---:|
| T0.1 | Structural | • **Achieve:** Scaffold the initial project tree.<br>• **Deliver:** `pyproject.toml`, `src/ragent/`, `tests/{unit,integration,e2e}/`. | — | [x] | Dev | W1 |
| T0.2 | Structural | • **Achieve:** Lock in CI quality gate with coverage floor (DoD).<br>• **Deliver:** `make check` alias = `ruff format . && ruff check . --fix && pytest --cov=src/ragent --cov-branch --cov-fail-under=92`; CI fails on coverage drop. | T0.1 | [x] | Dev | W1 |
| T0.3 | Red | • **Achieve:** Pin sortable, URL-safe ID contract.<br>• **Deliver:** `tests/unit/test_id_gen.py` — `new_id()` returns 26-char Crockford base32; sortable across calls. | T0.1 | [x] | QA | W2 |
| T0.4 | Green | • **Achieve:** Implement UUIDv7-based ID generator.<br>• **Deliver:** `src/ragent/utility/id_gen.py` (UUIDv7 → 16 bytes → base32; ≤ 30 LOC). | T0.3 | [x] | Dev | W2 |
| T0.5 | Red | • **Achieve:** Pin end-to-end UTC datetime contract.<br>• **Deliver:** `tests/unit/test_datetime_utility.py` — `utcnow()` tz-aware UTC; `to_iso` ends in `Z`; `from_db` attaches UTC. | T0.1 | [x] | QA | W2 |
| T0.6 | Green | • **Achieve:** Implement UTC datetime helpers.<br>• **Deliver:** `src/ragent/utility/datetime.py`. | T0.5 | [x] | Dev | W2 |
| T0.7 | Red | • **Achieve:** Pin document state-machine transitions (S10).<br>• **Deliver:** `tests/unit/test_state_machine.py` — accepts `{UPLOADED→PENDING, PENDING→READY, PENDING→FAILED, PENDING→DELETING, READY→DELETING, FAILED→DELETING}`; rejects `{UPLOADED→FAILED, READY→PENDING, FAILED→READY, DELETING→READY}`. | T0.1 | [x] | QA | W2 |
| T0.8 | Structural | • **Achieve:** Establish persistent schema for documents + chunks (B3).<br>• **Deliver:** `migrations/001_initial.sql` — `documents` (cols: `document_id CHAR(26) PK`, `create_user VARCHAR(64) NOT NULL` (B14), `source_id VARCHAR(128) NOT NULL` (B11), `source_app VARCHAR(64) NOT NULL` (B11), `source_title VARCHAR(256) NOT NULL` (B11), `source_workspace VARCHAR(64) NULL` (B11), `object_key VARCHAR(256) NOT NULL` (B25), `status ENUM(...)`, `attempt INT`, `created_at`, `updated_at`) + `chunks` (cols: `chunk_id`, `document_id`, `ord`, `text MEDIUMTEXT`, `lang VARCHAR(8)`).<br>• Indexes: `idx_status_updated`, `idx_source_app_id_status_created`, `idx_create_user_document`, `idx_document` (chunks). Alembic configured. | T0.1 | [x] | Dev | W2 |
| T0.8a | Structural | • **Achieve:** Keep a head-of-tree schema snapshot for drift detection (B3).<br>• **Deliver:** `migrations/schema.sql` — consolidated snapshot updated in lockstep with every `NNN_*.sql`. | T0.8 | [x] | Dev | W2 |
| T0.8e | Structural | • **Achieve:** Check in canonical ES index definition (B26).<br>• **Deliver:** `resources/es/chunks_v1.json` — full settings + mappings per spec §5.2 (icu_text analyzer, bbq_hnsw vector index). | T0.1 | [x] | Dev | W2 |
| T0.9 | Structural | • **Achieve:** Provide reusable testcontainer fixtures for all integration tests (B8).<br>• **Deliver:** `tests/conftest.py` — session-scoped fixtures for MariaDB / ES / Redis / MinIO.<br>• ES container uses a custom image with `analysis-icu` plugin pre-installed (e.g. `Dockerfile.es-test` extending `docker.elastic.co/elasticsearch/elasticsearch:9.2.3`); fixture exposes `ES_HOSTS`, `ES_USERNAME`, `ES_PASSWORD` (or `ES_API_KEY`) and `ES_VERIFY_CERTS=false` (B28).<br>• MinIO fixture exposes `MINIO_ENDPOINT/ACCESS_KEY/SECRET_KEY`, `MINIO_SECURE=false` (B28). MariaDB fixture builds `MARIADB_DSN` (B28).<br>• Redis container starts in standalone mode by default; sentinel topology covered by a separate compose-style fixture when `REDIS_MODE=sentinel` (B27). | T0.1 | [x] | Dev | W2 |
| T0.8b | Red | • **Achieve:** Guarantee `schema.sql` ≡ `alembic upgrade head` (B3 invariant).<br>• **Deliver:** `tests/integration/test_schema_drift.py` — apply both to two scratch DBs (testcontainers); `mysqldump` diff must be empty. | T0.8a, T0.9 | [x] | QA | W2 |
| T0.8f | Red | • **Achieve:** Prevent prose/resource drift on ES index (B26).<br>• **Deliver:** `tests/integration/test_es_resource_drift.py` — parse `resources/es/chunks_v1.json` and the JSON block in spec §5.2; assert deep-equal. Pure file diff; no testcontainer needed. | T0.8e | [x] | QA | W2 |
| T0.8c | Red | • **Achieve:** Verify idempotent first-boot auto-init across MariaDB + ES (B3, B4).<br>• **Deliver:** `tests/integration/test_bootstrap_auto_init.py` — first boot creates tables/indexes/`chunks_v1` from `resources/es/chunks_v1.json` (B26); second boot is a no-op; pre-existing schema drift is logged `event=schema.drift` and surfaces in `/readyz` as degraded. | T0.8a, T0.8e, T0.9 | [x] | QA | W2 |
| T0.8d | Green | • **Achieve:** Implement non-destructive schema bootstrap.<br>• **Deliver:** `src/ragent/bootstrap/init_schema.py` — `CREATE … IF NOT EXISTS` for MariaDB; for ES, read every `resources/es/*.json` and `PUT /<index>` if absent (B26); refuses to `ALTER`. | T0.8c | [x] | Dev | W2 |
| T0.8g | Red | • **Achieve:** Fail closed when ES `analysis-icu` plugin is missing (B26, I5).<br>• **Deliver:** `tests/integration/test_es_plugin_required.py` — `/readyz` returns 503 with `error_code=ES_PLUGIN_MISSING` listing `analysis-icu`; init refuses to `PUT` the index. | T0.8d, T0.9 | [x] | QA | W2 |
| T0.10 | Structural | • **Achieve:** Provide one canonical TaskIQ broker dispatching on Redis topology (B27/B28).<br>• **Deliver:** `src/ragent/bootstrap/broker.py` — module-level `broker` export.<br>• Standalone reads `REDIS_BROKER_URL`; sentinel uses `REDIS_SENTINEL_HOSTS` + `REDIS_BROKER_SENTINEL_MASTER`. Single canonical import path `from ragent.bootstrap.broker import broker` used by every `@broker.task` decorator (T3.2b, T3.2d) and by the worker entrypoint (T7.5d). | T0.1 | [x] | Dev | W2 |
| T0.10a | Red | • **Achieve:** Pin broker topology dispatch behavior (B27).<br>• **Deliver:** `tests/unit/test_broker_topology.py` — standalone uses `REDIS_BROKER_URL`; sentinel uses `REDIS_SENTINEL_HOSTS` + `REDIS_BROKER_SENTINEL_MASTER`; missing var in sentinel mode → SystemExit. | T0.10 | [x] | QA | W2 |
| T0.11 | Structural | • **Achieve:** Single source of truth for operator env config (B30).<br>• **Deliver:** `.env.example` at repo root enumerating **every** variable from spec §4.6 in the same subsection order (bootstrap → datastore → redis → clients → worker → pipeline → timeouts → observability).<br>• Required vars carry `# REQUIRED — set before boot`; default-overridable vars show defaults inline. Operator workflow: `cp .env.example .env` → fill required → start API + worker. | T0.1 | [x] | Dev | W2 |
| T0.11a | Red | • **Achieve:** CI gate against operator-config drift (B30).<br>• **Deliver:** `tests/unit/test_env_example_drift.py` — parse spec §4.6 tables and `.env.example`; assert symmetric variable set, defaults match, every `(required)` var in spec is marked `# REQUIRED` in example. | T0.11 | [x] | QA | W2 |

### Track T1 — Plugins (Protocol + Registry + Extractors)

| # | Category | Task | Depends On | Status | Owner | Week |
|---|---|---|---|:---:|---|:---:|
| T1.1 | Red | • **Achieve:** Pin plugin Protocol shape (S4).<br>• **Deliver:** `tests/unit/test_plugin_protocol.py` — protocol attribute/method conformance. | T0.1 | [x] | QA | W2 |
| T1.2 | Green | • **Achieve:** Provide runtime-checkable plugin Protocol.<br>• **Deliver:** `src/ragent/plugins/protocol.py` (`runtime_checkable` Protocol). | T1.1 | [x] | Dev | W2 |
| T1.3 | Red | • **Achieve:** Pin stub graph extractor no-op behavior (S5).<br>• **Deliver:** Stub graph extractor no-op test. | T0.1 | [x] | QA | W2 |
| T1.4 | Green | • **Achieve:** Implement stub graph extractor placeholder for P1.<br>• **Deliver:** `src/ragent/plugins/stub_graph.py`. | T1.3 | [x] | Dev | W2 |
| T1.5 | Refactor | • **Achieve:** Confirm no premature abstraction across plugins (YAGNI).<br>• **Deliver:** Review note — no shared boilerplate; kept duplicated. | T1.4 | [x] | Reviewer | W2 |
| T1.6 | Red | • **Achieve:** Pin registry semantics: register, fan_out, timeout, dup-detect (S11, R6, S29).<br>• **Deliver:** `tests/unit/test_plugin_registry.py` — `register`, `fan_out`, `all_required_ok`; duplicate name raises; per-plugin timeout `PLUGIN_FAN_OUT_TIMEOUT_SECONDS` (B28, default 60) overrun → `Result(error="timeout")`. | T1.2 | [x] | QA | W3 |
| T1.7 | Green | • **Achieve:** Implement plugin registry with concurrent fan-out.<br>• **Deliver:** `src/ragent/plugins/registry.py` (`PluginRegistry`, `Result`, `DuplicatePluginError`); concurrent fan_out with per-plugin timeout from `PLUGIN_FAN_OUT_TIMEOUT_SECONDS`. | T1.6 | [x] | Dev | W3 |
| T1.8 | Red | • **Achieve:** Pin `fan_out_delete` semantics: idempotent, no DB tx held (R10, P-E).<br>• **Deliver:** `tests/unit/test_plugin_registry_delete.py` — calls every registered plugin; per-plugin timeout `PLUGIN_FAN_OUT_TIMEOUT_SECONDS` (B28); idempotent on already-deleted. | T1.7 | [x] | QA | W3 |
| T1.9 | Red | • **Achieve:** Pin VectorExtractor contract: idempotent ingest, clean delete.<br>• **Deliver:** `tests/unit/test_vector_extractor.py` — Protocol conformance, embedder/ES bulk once, idempotent rerun, delete clears chunks. | T1.2 | [x] | QA | W3 |
| T1.10 | Green | • **Achieve:** Implement vector extractor plugin.<br>• **Deliver:** `src/ragent/plugins/vector.py`. | T1.9 | [x] | Dev | W3 |
| T1.11 | Red | • **Achieve:** Pin title-aware embedding + DI shape + ES doc fields (B15+B17+B29).<br>• **Deliver:** `tests/unit/test_vector_extractor_title.py` — constructor `VectorExtractor(repo, chunks, embedder, es)` (B17 DI).<br>• Mock embedder asserts input equals `f"{source_title}\n\n{chunk_text}"`.<br>• ES bulk body carries `title`, `text`, `lang`, `chunk_id`, `document_id`, `embedding`, `source_app`, `source_workspace` per `resources/es/chunks_v1.json` (B26, B29) — no extra/missing fields.<br>• `source_workspace` omitted when NULL (ES `keyword` missing; `term` filter does not match). `extract(document_id)` reads denormalised values via `repo.get`. Constructor injection only — no service-locator, no globals. | T1.10, T2.2, T0.8e | [x] | QA | W3+ |
| T1.12 | Green | • **Achieve:** Amend extractor for title-prefixed embedding + denormalised fields (B17, B29).<br>• **Deliver:** Updated `src/ragent/plugins/vector.py` — constructor `(repo, chunks, embedder, es)`; `extract` fetches doc, prepends `f"{source_title}\n\n"` before embedding, writes `title`, `source_app`, `source_workspace` (when non-null) into ES bulk doc. | T1.11 | [x] | Dev | W3+ |

### Track T2 — Ingest CRUD (Repositories + Storage + Service + Router)

| # | Category | Task | Depends On | Status | Owner | Week |
|---|---|---|---|:---:|---|:---:|
| T2.1 | Red  | • **Achieve:** Pin DocumentRepository CRUD + lock + lifecycle queries (B11, B14, B16, B17, B25, B29, R1/R3/R7, S28/S33).<br>• **Deliver:** `tests/unit/test_document_repository.py`.<br>• **Coverage:** `create` (mandatory `source_id`+`source_app`+`source_title`+`create_user`+`object_key`, optional `source_workspace`); `get` returns DocumentRow with all denormalised fields; `delete`; `list`.<br>• **Concurrency:** `acquire_nowait` (FOR UPDATE NOWAIT raises on lock contention); `list_ready_by_source` and `pop_oldest_loser_for_supersede` FOR UPDATE SKIP LOCKED LIMIT 1 (P-C single-loser-per-tx).<br>• **Lifecycle:** `update_status` (state-machine guarded); `update_heartbeat` bumps `updated_at` to NOW() (B16, S33); `list_pending_stale`, `list_uploaded_stale` (R1).<br>• **Hydration:** `find_multi_ready_groups` (R3); `get_sources_by_document_ids` for chat hydration (C4 rename); `list_by_create_user` for future "my uploads" (B14). | T0.4, T0.6, T0.7 | [x] | QA | W3 |
| T2.2 | Green | • **Achieve:** Implement document persistence layer (CRUD only).<br>• **Deliver:** `src/ragent/repositories/document_repository.py`. | T2.1 | [x] | Dev | W3 |
| T2.3 | Red  | • **Achieve:** Pin chunk persistence contract.<br>• **Deliver:** `tests/unit/test_chunk_repository.py` — `bulk_insert` / `delete_by_document_id`. | T0.4 | [x] | QA | W3 |
| T2.4 | Green | • **Achieve:** Implement chunk persistence layer.<br>• **Deliver:** `src/ragent/repositories/chunk_repository.py`. | T2.3 | [x] | Dev | W3 |
| T2.5 | Red  | • **Achieve:** Pin MinIO client object-key format, timeouts, and key-only return contract (B10, B25, C3).<br>• **Deliver:** `tests/unit/test_minio_client.py` — `put_object(source_app, source_id, document_id, ...)` builds key `{source_app}_{source_id}_{document_id}` (sanitised to `[A-Za-z0-9._-]`) in bucket from `MINIO_BUCKET` (default `ragent`, B10/B28).<br>• Upload bound by `MINIO_PUT_TIMEOUT_SECONDS` (B28, default 60); download by `MINIO_GET_TIMEOUT_SECONDS` (B28, default 30). Returns object key only, not URI. `delete_object(key)` idempotent. Bucket read once at startup; MinIO is transient staging only. | T0.1 | [x] | QA | W3 |
| T2.6 | Green | • **Achieve:** Implement MinIO client adapter.<br>• **Deliver:** `src/ragent/storage/minio_client.py` — B10 key format, B25 returns key string; per-call timeouts from `MINIO_PUT_TIMEOUT_SECONDS` / `MINIO_GET_TIMEOUT_SECONDS` (B28). | T2.5 | [x] | Dev | W3 |
| T2.7 | Red  | • **Achieve:** Pin ingest-create service contract: validation, MIME allow-list, atomic put+create+kiq, rollback on MinIO failure (B11, B25, C1, S23).<br>• **Deliver:** `tests/unit/test_ingest_service_create.py` — `source_id`/`source_app`/`source_title` mandatory (422 on missing); MIME validated against `{text/plain, text/markdown, text/html, text/csv}` (≤50 MB); MinIO put → `repo.create` (persists `source_id`, `source_app`, `source_title`, `object_key`, `create_user`, optional `source_workspace`) → kiq dispatch; row rolled back if MinIO put fails. | T2.2, T2.6, T1.7 | [x] | QA | W3 |
| T2.8 | Green | • **Achieve:** Implement ingest-create service path.<br>• **Deliver:** `src/ragent/services/ingest_service.py::create` (≤ 30 LOC/method). | T2.7 | [x] | Dev | W3 |
| T2.9 | Red  | • **Achieve:** Pin delete cascade order, idempotency, no-tx-during-fan-out (P-E, S13/S14).<br>• **Deliver:** `tests/unit/test_ingest_service_delete.py` — order: acquire NOWAIT → DELETING short tx → `fan_out_delete` OUTSIDE tx → chunks → MinIO (if UPLOADED/PENDING) → row.<br>• Failure leaves row in DELETING (S13); re-delete returns 204 idempotently (S14); `fan_out_delete` runs with no DB tx open. | T2.8, T1.8 | [x] | QA | W3 |
| T2.10 | Green | • **Achieve:** Implement delete cascade.<br>• **Deliver:** `src/ragent/services/ingest_service.py::delete`. | T2.9 | [x] | Dev | W3 |
| T2.11 | Red | • **Achieve:** Pin cursor pagination + limit clamp (S15).<br>• **Deliver:** `tests/unit/test_ingest_service_list.py` — cursor by `document_id` ASC; `?limit` upper-bounded by `INGEST_LIST_MAX_LIMIT` (B28, default 100); `limit=200` clamped or 422 per contract; `next_cursor` correctness; P1 OPEN — no ACL filter. | T2.2 | [x] | QA | W3 |
| T2.12 | Green | • **Achieve:** Implement list service path.<br>• **Deliver:** `src/ragent/services/ingest_service.py::list`. | T2.11 | [x] | Dev | W3 |
| T2.13 | Red | • **Achieve:** Pin router as thin parse/validate/delegate layer with RFC 9457 errors (B5, B11, S23).<br>• **Deliver:** `tests/unit/test_ingest_router.py` — 415 `INGEST_MIME_UNSUPPORTED` outside C1; 413 `INGEST_FILE_TOO_LARGE` over `INGEST_MAX_FILE_SIZE_BYTES` (B28, default 52428800); 422 on missing `source_id`/`source_app`/`source_title`; `X-User-Id` required (P1 OPEN).<br>• All non-2xx bodies are `application/problem+json` with `error_code`; 422 carries `errors[]`. | T2.8, T2.10, T2.12 | [x] | QA | W3 |
| T2.14 | Green | • **Achieve:** Implement ingest router + RFC 9457 problem builder.<br>• **Deliver:** `src/ragent/routers/ingest.py` + `src/ragent/errors/problem.py` (B5); declares all endpoints in spec §4.1. | T2.13 | [x] | Dev | W3 |

### Track T3 — Pipelines (Ingest + Chat assembly)

| # | Category | Task | Depends On | Status | Owner | Week |
|---|---|---|---|:---:|---|:---:|
| T3.1 | Red | • **Achieve:** Pin language-routed ingest pipeline shape (B1).<br>• **Deliver:** `tests/integration/test_ingest_pipeline.py` — Haystack `Convert→Clean→LanguageRouter→{cjk_splitter\|en_splitter} (sentence)→Embed`; mock embedder. EN doc → `en_splitter`; CJK → `cjk_splitter`; chunk count == sentence count. | T2.4, T4.2 | [x] | QA | W3 |
| T3.2 | Green | • **Achieve:** Implement ingest pipeline factory.<br>• **Deliver:** `src/ragent/pipelines/factory.py` + `pipelines/ingest.py`. | T3.1 | [x] | Dev | W3 |
| T3.2a | Red | • **Achieve:** Pin terminal-commit-before-MinIO-delete order (S16, S21).<br>• **Deliver:** `tests/integration/test_worker_minio_cleanup.py` — terminal status (`READY`/`FAILED`) committed before `MinIOClient.delete_object`; delete failure → still terminal + `event=minio.orphan_object`; on retry path (still PENDING) MinIO object retained. | T3.2 | [x] | QA | W3 |
| T3.2b | Green | • **Achieve:** Implement ingest worker task with two-tx envelope, heartbeat, timeout, post-commit cleanup (B16, B18, B25, R5, S27).<br>• **Deliver:** `@broker.task("ingest.pipeline")` (broker from T0.10).<br>• Flow: TX-A acquire NOWAIT + PENDING (commit) → start heartbeat (UPDATE `updated_at` every `WORKER_HEARTBEAT_INTERVAL_SECONDS`) → pipeline body OUTSIDE tx, `asyncio.wait_for(timeout=PIPELINE_TIMEOUT_SECONDS)` → cancel heartbeat in `finally` → TX-B terminal commit (FAILED also runs `fan_out_delete` + `delete_by_document_id`; PIPELINE_TIMEOUT sets `error_code=PIPELINE_TIMEOUT`) → post-commit MinIO best-effort delete.<br>• Plugin registry + repos resolved via composition root (T7.5a). | T3.2a, T0.10 | [x] | Dev | W3 |
| T3.2i | Red | • **Achieve:** Verify heartbeat suppresses live-worker re-dispatch (S33, B16).<br>• **Deliver:** `tests/integration/test_worker_heartbeat.py` — mid-pipeline 4-min sleep + stand-in Reconciler query at 5 min sees `updated_at < 30 s`, row excluded; on worker kill, `updated_at` ages past 5 min and same query returns row exactly once. | T3.2b, T2.2 | [x] | QA | W3 |
| T3.2j | Red | • **Achieve:** Pin pipeline-timeout failure path (S34, B18).<br>• **Deliver:** `tests/integration/test_pipeline_timeout.py` — body sleeping > `PIPELINE_TIMEOUT_SECONDS` → row `FAILED` with `error_code=PIPELINE_TIMEOUT`, cleanup ran, `event=ingest.failed reason=pipeline_timeout` logged. | T3.2b | [x] | QA | W3 |
| T3.2k | Red | • **Achieve:** Pin CSV row-merger branch keyed on MIME (S35, B24).<br>• **Deliver:** `tests/integration/test_csv_row_merger.py` — 10 000-row CSV (~50 chars each) → `chunks` count ≤ `ceil(total_chars / CSV_CHUNK_TARGET_CHARS)` (≈ 250); identical-size `.txt` produces sentence-level chunks. | T3.2 | [x] | QA | W3 |
| T3.2l | Green | • **Achieve:** Implement CSV-only RowMerger branch (B24).<br>• **Deliver:** Pipeline factory adds `RowMerger` SuperComponent on `text/csv` branch only (`ConditionalRouter` on MIME); merges rows joined by `\n` until buffer ≥ `CSV_CHUNK_TARGET_CHARS`. | T3.2k | [x] | Dev | W3 |
| T3.2c | Red | • **Achieve:** Pin supersede semantics: per-loser commits, MAX(created_at) survives, scoped by `(source_id, source_app)` (P-C, S17–S22, S31).<br>• **Deliver:** `tests/integration/test_supersede_task.py` — on `READY` kiqs `ingest.supersede`; pops oldest loser one-at-a-time, commits per-loser; same `source_id` different `source_app` coexists; out-of-order finish converges; FAILED never enqueues supersede; idempotent re-run. | T3.2b, T2.10 | [x] | QA | W3 |
| T3.2d | Green | • **Achieve:** Implement supersede service + worker without holding K row locks across K cascades.<br>• **Deliver:** `services/ingest_service.py::supersede(document_id)` + `@broker.task("ingest.supersede")`; loops `pop_oldest_loser_for_supersede` + cascade delete + commit. | T3.2c, T0.10 | [x] | Dev | W3 |
| T3.2e | Red | • **Achieve:** Guarantee retry idempotency — no duplicate chunks (R4, S25).<br>• **Deliver:** `tests/integration/test_pipeline_retry_idempotent.py` — Reconciler retry of partial ingest produces zero duplicates; pipeline first step is `delete_by_document_id` + `VectorExtractor.delete`. | T3.2 | [x] | QA | W3 |
| T3.2f | Green | • **Achieve:** Implement idempotency-clean prefix.<br>• **Deliver:** Pipeline factory prepends idempotency-clean step. | T3.2e | [x] | Dev | W3 |
| T3.2g | Red | • **Achieve:** Pin acquire-NOWAIT contention path: fail fast, no `attempt` increment (R7, S28).<br>• **Deliver:** `tests/unit/test_worker_acquire_nowait.py` — concurrent dispatch: second worker fails fast, re-kiqs self with backoff, `attempt` unchanged. | T3.2 | [x] | QA | W3 |
| T3.2h | Green | • **Achieve:** Implement NOWAIT + exponential backoff retry.<br>• **Deliver:** Worker uses `acquire_nowait`; on `LockNotAvailable` re-kiqs with exponential backoff (cap 30 s). | T3.2g | [x] | Dev | W3 |
| T3.3 | Red | • **Achieve:** Pin chat request validation, env-driven defaults, system auto-prepend, optional filters (B12, B22, B29, S6b/S6c/S6i).<br>• **Deliver:** `tests/unit/test_chat_request_schema.py` — `messages` required (422 `CHAT_MESSAGES_MISSING`); defaults from `RAGENT_DEFAULT_LLM_PROVIDER/MODEL/TEMPERATURE/MAX_TOKENS`; `provider` allow-list `{"openai"}` else 422 `CHAT_PROVIDER_UNSUPPORTED`; auto-prepend `RAGENT_DEFAULT_SYSTEM_PROMPT` if no system entry.<br>• Filters: `source_app` / `source_workspace` optional; both omitted ⇒ no filter; empty or > 64 chars ⇒ 422 `CHAT_FILTER_INVALID`. | T4.6 | [x] | QA | W4 |
| T3.4 | Green | • **Achieve:** Implement `ChatRequest` schema + helpers per B21/B22/B29.<br>• **Deliver:** `src/ragent/schemas/chat.py` (Pydantic) — `ChatRequest` with env-driven defaults + provider allow-list validator + optional `source_app`/`source_workspace` (length 1–64, snake_case); `Message`; `normalize_messages()` prepends default system. JSON naming per B21 (`maxTokens` camelCase; ID/scope fields snake_case). | T3.3 | [x] | Dev | W4 |
| T3.5 | Red | • **Achieve:** Pin hybrid retrieval pipeline: title-aware embedding, BM25 over `icu_text`, filters, source hydration returning full chunk content (B11, B15, B23, B26, B29, C4, S6f–S6h, S36).<br>• **Deliver:** `tests/integration/test_chat_pipeline_retrieval.py`.<br>• **Components:** `QueryEmbedder` → `ESVector` kNN + `ESBM25 multi_match(["text","title^2"])` with `icu_text` → `DocumentJoiner` (`CHAT_JOIN_MODE`) → `SourceHydrator` joins `documents` for `(source_app, source_id, source_title)` and **returns full chunk content untruncated** (B23 revised: truncation is router-layer only).<br>• **Recall:** title-only lexical recalls via BM25 `title^2`; semantic title query recalls via title-aware embedding (B15).<br>• **CJK BM25 (S36):** `"產品規格"` recalls via `icu_tokenizer` and not via `standard` analyzer control.<br>• **Filters (S6f–S6h):** `source_app` set ⇒ both retrievers carry `term: {source_app: <v>}`; combined with `source_workspace` ANDs; non-matching ⇒ empty (`sources: null` at router); empty index ⇒ empty. | T2.4, T4.2, T4.4, T2.2, T1.12 | [x] | QA | W4 |
| T3.5a | Red | • **Achieve:** Pin pipeline-graph dispatch by `CHAT_JOIN_MODE` (C6).<br>• **Deliver:** `tests/unit/test_pipeline_factory_join_mode.py` — for each value ∈ {`rrf`, `concatenate`, `vector_only`, `bm25_only`} factory returns expected component graph (e.g. `vector_only` has no BM25 retriever and no joiner). Default `rrf`. | T3.5 | [x] | QA | W4 |
| T3.6 | Green | • **Achieve:** Implement chat retrieval pipeline + shared retrieval utilities (B23 revised).<br>• **Deliver:** `src/ragent/pipelines/chat.py::build_retrieval_pipeline(join_mode)` + `SourceHydrator` (returns full chunk content; truncation moved to router layer) + shared `build_es_filters(source_app, source_workspace)` + `doc_to_source_entry(doc)` utilities; `EXCERPT_MAX_CHARS` exported as public constant. Factory dispatches on `CHAT_JOIN_MODE`. | T3.5, T3.5a | [x] | Dev | W4 |
| T3.7 | Red | • **Achieve:** Pin non-streaming LLM client contract (B28).<br>• **Deliver:** `tests/unit/test_llm_client_chat.py` — `LLMClient.chat(messages,...)` returns `{content, usage:{promptTokens,completionTokens,totalTokens}}`; honours `model`/`temperature`/`maxTokens`; timeout `LLM_TIMEOUT_SECONDS` (default 120). | T4.6 | [x] | QA | W4 |
| T3.8 | Green | • **Achieve:** Implement non-streaming `chat()` and ensure streaming surfaces usage.<br>• **Deliver:** `src/ragent/clients/llm.py::chat()` (timeout `LLM_TIMEOUT_SECONDS`); streaming requests `stream_options.include_usage=true` so terminal `done` carries usage. | T3.7 | [x] | Dev | W4 |
| T3.9 | Red | • **Achieve:** Pin `POST /chat` non-streaming response shape (B5, B12, B13, S6a/S6d/S6e).<br>• **Deliver:** `tests/integration/test_chat_endpoint.py` — 200 `application/json` per §3.4.2 (`content`, `usage`, `model`, `provider`, `sources[]`); each `sources[]` has 6 fields + `type="knowledge"`; empty retrieval ⇒ `sources: null`; LLM error ⇒ RFC 9457 problem+json. | T3.6, T3.8 | [x] | QA | W4 |
| T3.10 | Green | • **Achieve:** Implement non-streaming chat endpoint.<br>• **Deliver:** `src/ragent/routers/chat.py::POST /chat`. | T3.9 | [x] | Dev | W4 |
| T3.11 | Red | • **Achieve:** Pin SSE streaming framing (B6, B12, S6).<br>• **Deliver:** `tests/integration/test_chat_stream_endpoint.py` — emits ≥1 `data: {"type":"delta",...}` then exactly one `data: {"type":"done", ...§3.4.2 body...}`; mid-stream LLM failure → single `data: {"type":"error","error_code","message"}` then close (no `event: error`); pre-stream failure ⇒ RFC 9457 problem+json. | T3.6, T3.8 | [x] | QA | W4 |
| T3.12 | Green | • **Achieve:** Implement SSE chat stream endpoint.<br>• **Deliver:** `src/ragent/routers/chat.py::POST /chat/stream` — delta/done/error framing per §3.4.3. | T3.11 | [x] | Dev | W4 |
| T3.13 | Red | • **Achieve:** Pin fixed-window per-key rate-limit primitive against Redis (B27, B31).<br>• **Deliver:** `tests/unit/test_rate_limiter.py` — `check(key, limit, window_seconds) → RateLimitResult(allowed, remaining, reset_at)`; under limit ⇒ allowed, at limit ⇒ blocked + `reset_at = now + remaining_window`; window expiry resets. Uses `INCR` + `EXPIRE` against `REDIS_RATELIMIT_*`; standalone + sentinel covered. | T0.10, T0.9 | [x] | QA | W4 |
| T3.14 | Green | • **Achieve:** Implement RateLimiter adapter + composition wiring (B31).<br>• **Deliver:** `src/ragent/clients/rate_limiter.py` reading `REDIS_RATELIMIT_URL` (standalone) or `REDIS_RATELIMIT_SENTINEL_MASTER` + `REDIS_SENTINEL_HOSTS` (sentinel) from composition root; key prefix `ratelimit:`. Composition root exports `chat_rate_limit_dep` `Depends` factory bound to `CHAT_RATE_LIMIT_PER_MINUTE` / `CHAT_RATE_LIMIT_WINDOW_SECONDS`. | T3.13 | [x] | Dev | W4 |
| T3.15 | Red | • **Achieve:** Pin per-user rate-limit behavior end-to-end (B31, S37).<br>• **Deliver:** `tests/integration/test_chat_rate_limit.py` — with `CHAT_RATE_LIMIT_PER_MINUTE=N`, N+1 calls from same `X-User-Id`: first N → 200, (N+1)th → 429 problem+json `CHAT_RATE_LIMITED` + `Retry-After`; same for `/chat/stream`. Different user gets fresh budget; window expiry resets counter. | T3.14, T3.10, T3.12 | [x] | QA | W4 |
| T3.16 | Green | • **Achieve:** Apply rate-limit dependency to chat surfaces only (B31).<br>• **Deliver:** Wire `Depends(chat_rate_limit_dep)` onto chat router for `/chat` and `/chat/stream`; health/ingest/MCP NOT rate-limited (router-level dependency, not global middleware). | T3.15 | [x] | Dev | W4 |
| T3.17 | Red | • **Achieve:** Pin RAG message construction: grounding system message + `=== CONTEXT START/END ===` user wrap.<br>• **Deliver:** `tests/unit/test_build_rag_messages.py` + extensions to chat endpoint/stream tests.<br>• **Behavior:** when retrieval ≥1 doc, LLM receives leading grounded system message + last user wrapped with context delimiters then verbatim query; each rendered chunk includes `source_app`, `source_title`, `document_id`, excerpt.<br>• **Edge cases:** with user-supplied system message, grounding rules prepend as a SEPARATE system message; empty retrieval ⇒ T3.3 default-system path with user message NOT wrapped. | T3.10, T3.12 | [x] | QA  | W4 |
| T3.18 | Green | • **Achieve:** Implement RAG message builder + wire into routers.<br>• **Deliver:** `src/ragent/schemas/chat.py::build_rag_messages` + `_render_context` + `_wrap_last_user`; routers swap `normalize_messages` for `build_rag_messages` and run retrieval before message construction. New env vars `RAGENT_DEFAULT_RAG_SYSTEM_PROMPT` and `RAGENT_RAG_GROUNDING_RULES` (defaults wired; `.env.example` updated). | T3.17 | [x] | Dev | W4 |
| T3.19 | Red | • **Achieve:** Pin standalone retrieve endpoint: filters, dedupe, excerpt truncated at router (§3.4.4, B23, S38–S41).<br>• **Deliver:** `tests/unit/test_retrieve_router.py` — `query` required; `source_app`/`source_workspace` optional (same B29 semantics, same length validators as ChatRequest); `dedupe=false` → all ranked chunks (same `document_id` may repeat); `dedupe=true` → one chunk per `document_id` (highest-scored, first-seen in score-sorted output); empty ⇒ `{"chunks":[]}` (not `null`); `excerpt` truncated to `EXCERPT_MAX_CHARS` in router. | T3.6, T3.10 | [x] | QA | W4 |
| T3.20 | Green | • **Achieve:** Implement `POST /retrieve` using shared pipeline utilities.<br>• **Deliver:** `src/ragent/routers/retrieve.py` — `RetrieveRequest` (`query`, `source_app`, `source_workspace`, `dedupe: bool = False`, same filter validators as `ChatRequest`); uses `build_es_filters` + `doc_to_source_entry` from `pipelines/chat`; `_dedupe_by_document` post-step; wired into `bootstrap/app.py` via `create_retrieve_router(retrieval_pipeline)`. | T3.19 | [x] | Dev | W4 |

### Track T4 — Third-Party Clients

| # | Category | Task | Depends On | Status | Owner | Week |
|---|---|---|---|:---:|---|:---:|
| T4.1 | Red | • **Achieve:** Pin token-manager refresh boundary, single-flight, J1-key body, ISO-8601 expiry, K8s SA mode, and credential safety (S9, P-F).<br>• **Deliver:** `tests/unit/test_token_manager.py` — POSTs `{"key": j1_token}` to `AI_API_AUTH_URL/auth/api/accesstoken`; parses `{"token": …, "expiresAt": "ISO-8601"}` response; refreshes at `expiresAt − 5min` boundary using fake clock; 100 concurrent calls share one HTTP exchange; K8s SA mode reads token from file; J1 token never appears in exception text. | T0.6 | [x] | QA | W4 |
| T4.2 | Green | • **Achieve:** Implement TokenManager with single-flight refresh per rule.md §"LLM & Embedding & Re-rank Auth API".<br>• **Deliver:** `src/ragent/clients/auth.py` (`TokenManager`) — `j1_token` or `k8s_sa_token_path`; `{"key": j1}` POST body; ISO-8601 `expiresAt` parse; `threading.Lock` single-flight. `composition.py` wires 3 separate managers (`AI_LLM/EMBEDDING/RERANK_API_J1_TOKEN`) or single shared SA manager when `AI_USE_K8S_SERVICE_ACCOUNT_TOKEN=true`. | T4.1 | [x] | Dev | W4 |
| T4.3 | Red | • **Achieve:** Pin embedding-client batching, asymmetric ingest/query timeouts, retry policy (B28, C8, P-B).<br>• **Deliver:** `tests/unit/test_embedding_client.py` — POST shape, `bge-m3`, validates `returnCode == 96200`, retry 3× @ 1 s.<br>• Batch interface accepts `list[str]`; one HTTP call per batch up to `EMBEDDER_BATCH_SIZE` (default 32). Ingest bound by `EMBEDDER_INGEST_TIMEOUT_SECONDS` (default 30); query bound by `EMBEDDER_QUERY_TIMEOUT_SECONDS` (default 10). | T4.2 | [x] | QA | W4 |
| T4.4 | Green | • **Achieve:** Implement embedding client with two timeout paths.<br>• **Deliver:** `src/ragent/clients/embedding.py` — batch size from `EMBEDDER_BATCH_SIZE`; ingest/query call paths use `EMBEDDER_INGEST_TIMEOUT_SECONDS` / `EMBEDDER_QUERY_TIMEOUT_SECONDS`. | T4.3 | [x] | Dev | W4 |
| T4.5 | Red | • **Achieve:** Pin LLM streaming contract + retry policy (B28).<br>• **Deliver:** `tests/unit/test_llm_client.py` — streaming async iterator yields deltas; timeout `LLM_TIMEOUT_SECONDS` (default 120); retry 3× @ 2 s. | T4.2 | [x] | QA | W4 |
| T4.6 | Green | • **Achieve:** Implement streaming LLM client.<br>• **Deliver:** `src/ragent/clients/llm.py` (timeout from `LLM_TIMEOUT_SECONDS`). | T4.5 | [x] | Dev | W4 |
| T4.7 | Red | • **Achieve:** Pin rerank client request shape (wired in P2).<br>• **Deliver:** `tests/unit/test_rerank_client.py` — POST shape, `bge-reranker-base`, `top_k=2`. | T4.2 | [x] | QA | W4 |
| T4.8 | Green | • **Achieve:** Implement rerank client.<br>• **Deliver:** `src/ragent/clients/rerank.py`. | T4.7 | [x] | Dev | W4 |

### Track T5 — Resilience (Reconciler)

| # | Category | Task | Depends On | Status | Owner | Week |
|---|---|---|---|:---:|---|:---:|
| T5.1 | Red | • **Achieve:** Pin PENDING re-dispatch + heartbeat suppression (S2, S33).<br>• **Deliver:** `tests/integration/test_reconciler_redispatch.py` — PENDING row with `updated_at < NOW() - RECONCILER_PENDING_STALE_SECONDS` (B16/B28, default 5 min) → re-kiq, idempotent; live worker with fresh `updated_at` is NOT re-dispatched. | T2.2, T2.8 | [x] | QA | W6 |
| T5.2 | Green | • **Achieve:** Implement one-shot Reconciler with K8s CronJob (B9).<br>• **Deliver:** `src/ragent/reconciler.py` — entrypoint `python -m ragent.reconciler`; `SELECT … FOR UPDATE SKIP LOCKED`. Manifest `deploy/k8s/reconciler-cronjob.yaml` (`*/5 * * * *`, `concurrencyPolicy: Forbid`) checked in. | T5.1 | [x] | Dev | W6 |
| T5.3 | Red | • **Achieve:** Pin attempt-budget exhaustion → FAILED with alert (S3).<br>• **Deliver:** `tests/integration/test_reconciler_failed.py` — `attempt > WORKER_MAX_ATTEMPTS` (B28, default 5) → status=FAILED + structured-log alert. | T5.2 | [x] | QA | W6 |
| T5.4 | Green | • **Achieve:** Implement FAILED transition + alert log line.<br>• **Deliver:** Status transition + structured log line `event=ingest.failed`. | T5.3 | [x] | Dev | W6 |
| T5.5 | Red | • **Achieve:** Pin DELETING resume idempotency (S13).<br>• **Deliver:** `tests/integration/test_reconciler_delete_resume.py` — DELETING row with `updated_at < NOW() - RECONCILER_DELETING_STALE_SECONDS` (B28, default 300) → resume cascade idempotently. | T2.10 | [x] | QA | W6 |
| T5.6 | Green | • **Achieve:** Implement DELETING resume.<br>• **Deliver:** Reconciler resumes DELETING. | T5.5 | [x] | Dev | W6 |
| T5.7 | Red | • **Achieve:** Pin UPLOADED-orphan re-dispatch (R1, S24).<br>• **Deliver:** `tests/integration/test_reconciler_uploaded_orphan.py` — UPLOADED row with `updated_at < NOW() - RECONCILER_UPLOADED_STALE_SECONDS` (B28, default 300) → re-kiq `ingest.pipeline`. | T2.8 | [x] | QA | W6 |
| T5.8 | Green | • **Achieve:** Implement UPLOADED-stale arm.<br>• **Deliver:** Reconciler arm for `UPLOADED > 5 min`. | T5.7 | [x] | Dev | W6 |
| T5.9 | Red | • **Achieve:** Pin multi-READY repair via supersede (R3, S26).<br>• **Deliver:** `tests/integration/test_reconciler_multi_ready_repair.py` — two READY rows for same `(source_id, source_app)` → re-enqueue `ingest.supersede`. | T3.2d | [x] | QA | W6 |
| T5.10 | Green | • **Achieve:** Implement multi-READY detector arm.<br>• **Deliver:** Reconciler arm `GROUP BY source_id, source_app HAVING COUNT(*)>1` → kiq supersede. | T5.9 | [x] | Dev | W6 |
| T5.11 | Red | • **Achieve:** Guarantee FAILED leaves no partial chunks/ES (R5, S27).<br>• **Deliver:** `tests/integration/test_reconciler_failed_cleanup.py` — on `attempt>5 → FAILED`, partial chunks/ES cleared. | T5.4 | [x] | QA | W6 |
| T5.12 | Green | • **Achieve:** Wire fan-out cleanup into FAILED transition.<br>• **Deliver:** FAILED transition runs `fan_out_delete` + `delete_by_document_id` before commit. | T5.11 | [x] | Dev | W6 |
| T5.13 | Red | • **Achieve:** Pin Reconciler tick observability (R8, S30).<br>• **Deliver:** `tests/integration/test_reconciler_heartbeat.py` — every tick increments `reconciler_tick_total` and emits `event=reconciler.tick`. | T5.2 | [x] | QA | W6 |
| T5.14 | Green | • **Achieve:** Implement tick counter + log line.<br>• **Deliver:** Heartbeat counter + log line in `reconciler.py`. | T5.13 | [x] | Dev | W6 |

### Track T6 — MCP Schema (501 in P1)

| # | Category | Task | Depends On | Status | Owner | Week |
|---|---|---|---|:---:|---|:---:|
| T6.1 | Structural | • **Achieve:** Reserve MCP surface in P1 with explicit 501 (§4.1.2).<br>• **Deliver:** `src/ragent/routers/mcp.py` exposing `POST /mcp/tools/rag` that returns 501 `application/problem+json` with `error_code=MCP_NOT_IMPLEMENTED`; mounted by `create_app` (T7.5c); OpenAPI auto-generated from FastAPI signature. | T2.14 | [x] | Dev | W6 |
| T6.2 | Red | • **Achieve:** Pin P1 MCP 501 contract (S8).<br>• **Deliver:** `tests/unit/test_mcp_endpoint.py` — returns 501 in P1. | T6.1 | [x] | QA | W6 |

### Track T7 — Observability + Acceptance

| # | Category | Task | Depends On | Status | Owner | Week |
|---|---|---|---|:---:|---|:---:|
| T7.1 | Refactor | • **Achieve:** Wire OTEL traces + Prometheus metrics across api/worker/reconciler.<br>• **Deliver:** Haystack auto-trace + FastAPI middleware (no custom spans). OTLP exporter from `OTEL_EXPORTER_OTLP_ENDPOINT` (B28; export disabled when unset); service name from `OTEL_SERVICE_NAME` per process (`ragent-api` / `ragent-worker` / `ragent-reconciler`); sampling from `OTEL_TRACES_SAMPLER` + `OTEL_TRACES_SAMPLER_ARG`.<br>• Logs include `auth_mode=open` field. Counters: `reconciler_tick_total`, `minio_orphan_object_total`, `multi_ready_repaired_total`. Histogram: `worker_pipeline_duration_seconds`. | T3.4 | [x] | SRE | W6 |
| T7.1a | Red | • **Achieve:** Pin alerting on stalled Reconciler ticks (R8, S30).<br>• **Deliver:** `tests/integration/test_alerting_rules.py` — Prometheus rule fires when `reconciler_tick_total` flat for > 10 min. | T7.1 | [x] | QA | W6 |
| T7.1b | Behavioral | • **Achieve:** Adopt structlog for ISO 8601 / API trace / business / error logs with OTEL correlation across api/worker/reconciler and chat/retrieve flows; pin Haystack 2.x content tracing off (Logging Rule).<br>• **Deliver:** `src/ragent/bootstrap/logging_config.py` (`configure_logging`, `LOG_FORMAT=json\|console`, denylist redaction); `src/ragent/middleware/logging.py` (`RequestLoggingMiddleware`); router-level OTEL spans + business logs in `routers/chat.py` and `routers/retrieve.py`; client spans in `clients/{llm,embedding,rerank}.py`; `setup_tracing` pins `is_content_tracing_enabled` (gated by `HAYSTACK_CONTENT_TRACING_ENABLED`).<br>• Tests: `tests/unit/test_logging_config.py`, `test_logging_redaction.py`, `test_logging_middleware.py`, `test_chat_retrieve_tracing.py`, `test_clients_tracing.py`. | T7.1 | [x] | SRE | W7 |
| T7.2 | Acceptance | • **Achieve:** Validate the operator quickstart promise end-to-end (B30).<br>• **Deliver:** `tests/e2e/test_ingest_success_rate.py` — fresh testcontainers + populated `.env` (from `.env.example`, T0.11) → launch real `python -m ragent.api` (T7.5d) and `python -m ragent.worker` (T7.5e) as subprocesses (no test-only scaffolding) → POST 100 documents to `/ingest` → poll `/ingest/{id}` → ≥ 99% reach `READY` within 60 s each.<br>• Asserts: only steps required are `cp .env.example .env`, fill required vars, start API + worker. | T3.2, T5.6, T7.5d, T7.5e, T0.11 | [x] | QA | W6 |
| T7.3 | Acceptance | • **Achieve:** Hit golden-set retrieval target on the live API process (C7).<br>• **Deliver:** `tests/e2e/test_golden_set.py` — top-3 ≥ 70% on 50 Q/A/expected_doc_id rows from `tests/e2e/golden_set.jsonl`; invoked against the same launched API process as T7.2 (`POST /chat` at `http://127.0.0.1:8000/chat`). | T3.4, T7.2 | [x] | QA | W6 |
| T7.4 | Acceptance | • **Achieve:** Validate chaos recovery SLA.<br>• **Deliver:** Chaos drill: kill worker mid-ingest → Reconciler recovers ≤ 10 min. | T5.6 | [x] | SRE | W6 |
| T7.3.x | Acceptance | • **Achieve:** Wire the T7.3 retrieval-recall SLO to a real automated gate against live embedder/rerank/LLM endpoints (closes the gap noted in `docs/00_journal.md::E2E gate integrity`).<br>• **Deliver:** (a) Scheduled / label-triggered CI job that exports real `EMBEDDING_API_URL` / `LLM_API_URL` / `RERANK_API_URL` + tokens via secrets, runs `make test-e2e-golden`; (b) decision-log row pinning which endpoint identity is used + cost expectation per run; (c) `tests/e2e/test_golden_set.py::test_golden_set_top3_accuracy_at_least_70pct` xfail flips to a hard assertion on that job. Default WireMock e2e remains the per-PR gate. | T7.3 | [ ] | QA | P2 |
| T7.4.x | Acceptance | • **Achieve:** Replace the single happy-path chaos test (currently xfail run=False, `tests/e2e/test_chaos_worker_kill.py`) with a partial-failure suite covering the cross-storage failure modes that motivated the test.<br>• **Deliver:** (a) Reconciler refactor — engine-per-tick or persistent daemon — so `Reconciler.run()` no longer reuses a closed asyncio loop (unblocks the existing test); (b) chaos cases from the SRE journal — at minimum: worker SIGKILL after PENDING; worker crash between MariaDB READY commit and ES bulk; worker crash between ES bulk and MariaDB commit; ES bulk 207 partial failure (WireMock-injected); reconciler watchdog absence.<br>• Each case lives under `tests/e2e/test_chaos/test_<case>.py` with its own fault-injection mechanism (env var or WireMock stub); suite runs in a slow-job CI lane separate from the per-PR gate. | T5.6, T7.4 | [ ] | SRE | P2 |
| T7.5 | Structural | • **Achieve:** Refuse to boot in any unsafe P1 OPEN configuration (B28).<br>• **Deliver:** `src/ragent/bootstrap/guard.py` — refuses to start unless `RAGENT_AUTH_DISABLED=true AND RAGENT_ENV=dev`; reads `RAGENT_HOST` (default `127.0.0.1`, refuses non-loopback in P1 OPEN) + `RAGENT_PORT` (default 8000); configures `LOG_LEVEL` (default `INFO`) for app + TaskIQ + Reconciler logging. Imported by both api and worker entrypoints. | T2.14 | [x] | SRE | W6 |
| T7.5a | Structural | • **Achieve:** Single composition root that owns all env reads + DI graph (B17, B30, B31, C6).<br>• **Deliver:** `src/ragent/bootstrap/composition.py`.<br>• **Singletons:** `TokenManager`, `EmbeddingClient`, `LLMClient`, `RerankClient`, `MinIOClient`, `Elasticsearch`, MariaDB engine, `RateLimiter`.<br>• **Wiring:** instantiates `DocumentRepository` / `ChunkRepository`; constructs plugins with DI args (`VectorExtractor(repo, chunks, embedder, es)`, `StubGraphExtractor()`); registers required + optional plugins onto one `PluginRegistry`; builds chat pipeline factory parameterised by `CHAT_JOIN_MODE` (C6).<br>• **Exports:** `Container` dataclass + `get_container()` accessor consumed by FastAPI `Depends` (T2.14, T3.10, T3.12, T7.8), worker tasks (T3.2b, T3.2d), Reconciler (T5.2); `chat_rate_limit_dep` `Depends` factory bound to `CHAT_RATE_LIMIT_PER_MINUTE` / `CHAT_RATE_LIMIT_WINDOW_SECONDS` (B31).<br>• **Invariant:** all env-var reads happen here, not inside business modules. | T1.7, T1.12, T2.2, T2.4, T2.6, T3.6, T3.8, T3.14, T4.2, T4.4, T4.6, T4.8 | [x] | Dev | W6 |
| T7.5b | Red | • **Achieve:** Verify composition root builds a fully-wired graph eagerly (B17, B30).<br>• **Deliver:** `tests/integration/test_composition_root.py` — `container.registry.plugins["vector"]` is a `VectorExtractor` whose `__init__` received the same `repo`/`chunks`/`embedder`/`es` instances exposed on the container (DI verified by identity); `container.chat_pipeline_factory(join_mode="rrf")` returns a runnable Haystack pipeline; missing required env var → SystemExit at construction time. | T7.5a | [x] | QA | W6 |
| T7.5c | Structural | • **Achieve:** Implement the FastAPI app factory with lifespan-driven init + RFC 9457 errors + `X-User-Id` middleware (B5, C9).<br>• **Deliver:** `src/ragent/bootstrap/app.py::create_app() -> FastAPI` — calls `guard.enforce()` (T7.5), constructs `Container` (T7.5a), registers `lifespan` hook that runs `init_schema()` (T0.8d) before traffic, mounts routers (`ingest` T2.14, `chat` T3.10/T3.12, `mcp` T6.x, `health` T7.8), wires `Depends`, installs problem+json error handler and `X-User-Id` middleware that excludes `/livez`/`/readyz`/`/metrics`. | T7.5, T7.5a, T0.8d, T2.14, T3.10, T3.12, T7.8 | [x] | Dev | W6 |
| T7.5d | Structural | • **Achieve:** Provide the single API entrypoint operators run.<br>• **Deliver:** `src/ragent/api.py` (`python -m ragent.api`) — uvicorn launcher: imports `bootstrap.app:create_app`, binds to `RAGENT_HOST:RAGENT_PORT`, log level from `LOG_LEVEL`. | T7.5c | [x] | Dev | W6 |
| T7.5e | Structural | • **Achieve:** Provide the single worker entrypoint operators run.<br>• **Deliver:** `src/ragent/worker.py` (`python -m ragent.worker`) — calls `guard.enforce()`, constructs the same `Container` (T7.5a), imports `ragent.bootstrap.broker:broker` (T0.10) and task modules (`ragent.workers.ingest`, `ragent.workers.supersede`) so `@broker.task` decorators register, runs the broker. Calls `init_schema()` defensively (idempotent) so worker can start before API. | T0.10, T7.5a, T0.8d, T3.2b, T3.2d | [x] | Dev | W6 |
| T7.5f | Red | • **Achieve:** Verify app factory boots end-to-end with lifespan + middleware.<br>• **Deliver:** `tests/integration/test_app_factory.py` — `create_app()` against testcontainers boots; lifespan runs `init_schema` exactly once; routers mounted at expected paths; `/livez` reachable without `X-User-Id`; protected route returns 422 problem+json on missing `X-User-Id`. | T7.5c | [x] | QA | W6 |
| T7.6 | Red | • **Achieve:** Pin startup-guard refusals.<br>• **Deliver:** `tests/unit/test_bootstrap_startup_guard.py` — non-dev env or `RAGENT_AUTH_DISABLED` unset → SystemExit; `RAGENT_HOST` other than `127.0.0.1` while `RAGENT_AUTH_DISABLED=true` → SystemExit; invalid `LOG_LEVEL` → SystemExit; happy path boots with all three vars at defaults. | T7.5 | [x] | QA | W6 |
| T7.7 | Red | • **Achieve:** Pin health/metrics surfaces and dependency probes (B4, B26, B27, B28, C9).<br>• **Deliver:** `tests/integration/test_health_endpoints.py`.<br>• **Endpoints:** `GET /livez` → 200 always; `GET /readyz` → 200 only when all probes pass within `READYZ_PROBE_TIMEOUT_SECONDS` (default 2 s); `GET /metrics` → 200 `text/plain; version=0.0.4`.<br>• **Probes:** MariaDB (`SELECT 1`); ES (cluster health + `analysis-icu` plugin + every `resources/es/*.json` index exists); Redis broker (`PING` against `REDIS_MODE` topology); Redis rate-limiter (`PING`, separate instance); MinIO (`ListBuckets`).<br>• **Failure mapping:** one dep down → 503 problem+json with `error_code` matching `ES_PLUGIN_MISSING` / `ES_INDEX_MISSING` / `SCHEMA_DRIFT` per §4.1.2 catalog.<br>• **Metrics exposed:** `reconciler_tick_total`, `worker_pipeline_duration_seconds`, `minio_orphan_object_total`, `multi_ready_repaired_total`.<br>• **Middleware bypass (C9):** all three endpoints reachable WITHOUT `X-User-Id`. | T7.1 | [x] | QA | W6 |
| T7.8 | Green | • **Achieve:** Implement health + metrics endpoints with per-dep timeouts and middleware bypass (B27, B28, C9).<br>• **Deliver:** `src/ragent/routers/health.py` (`/livez`, `/readyz`, `/metrics`); probes via `asyncio.wait_for(..., timeout=READYZ_PROBE_TIMEOUT_SECONDS)`; broker + rate-limiter Redis pinged independently per `REDIS_*_URL` / `REDIS_*_SENTINEL_MASTER`; `X-User-Id` middleware excludes the three health paths. | T7.7 | [x] | Dev | W6 |

### Track T8 — Authentication & Permission Layer `[~] DISABLED IN P1`

> P1 produces NO code in this track. Authentication (JWT) and Permission (OpenFGA via `PermissionClient` Protocol) are **separate layers**; ES carries no auth fields in any phase (B14). Interface in `00_spec.md` §3.5; implementation lands in P2 (see Phase 2 row P2.2).

| # | Category | Task | Depends On | Status | Owner | Phase |
|---|---|---|---|:---:|---|:---:|
| T8.1 | Red  | • **Achieve:** Pin JWT validation contract for all `/chat*` and `/ingest*` endpoints.<br>• **Deliver:** `tests/unit/test_jwt.py` — JWT shape `{"exp": <unix>, "preferred_username": "<user_id>"}`: expired `exp` → 401 `AUTH_TOKEN_EXPIRED`; missing `exp`/`preferred_username` → 401 `AUTH_CLAIM_MISSING`; non-numeric `exp` → 401 `AUTH_TOKEN_INVALID`; invalid signature → 401 `AUTH_TOKEN_INVALID`. | (entry) | [~] | QA | P2 |
| T8.2 | Green | • **Achieve:** Implement JWT FastAPI dependency with optional X-User-Id trust mode (§3.5).<br>• **Deliver:** `src/ragent/auth/jwt.py` — validates `exp`, extracts `preferred_username` → `user_id`. When `RAGENT_TRUST_X_USER_ID_HEADER=true`, bypasses JWT and reads `X-User-Id` as `preferred_username`. | T8.1 | [~] | Dev | P2 |
| T8.3 | Red  | • **Achieve:** Pin Permission Protocol surface.<br>• **Deliver:** `tests/unit/test_permission_client_protocol.py` — Protocol conformance for `batch_check(user_id, document_ids, relation) -> set[str]` and `list_objects(user_id, relation) -> list[str] \| None`. | T8.2 | [~] | QA | P2 |
| T8.4 | Green | • **Achieve:** Implement Permission Protocol + OpenFGA adapter (B14).<br>• **Deliver:** `src/ragent/auth/permission.py` — `PermissionClient` Protocol + `OpenFGAPermissionClient` (sole module importing the OpenFGA SDK). | T8.3 | [~] | Dev | P2 |
| T8.5 | Red  | • **Achieve:** Pin chat permission gate as opt-in post-retrieval filter; ES carries no auth fields (B14).<br>• **Deliver:** `tests/integration/test_chat_permission_gate.py` — with `RAGENT_PERMISSION_CHAT_ENABLED=true`, retrieval over-fetches K' candidates; `PermissionClient.batch_check(user_id=preferred_username, …)` post-filters to allowed; user A's query never surfaces user B's private doc. Flag `false` (default) bypasses gate. ES query body carries NO auth filter in either case. | T8.4 | [~] | QA | P2 |
| T8.6 | Green | • **Achieve:** Wire opt-in chat permission gate.<br>• **Deliver:** `pipelines/chat.py` post-retrieval gate (between SourceHydrator and LLM, or before hydration); no-op unless `RAGENT_PERMISSION_CHAT_ENABLED=true`. | T8.5 | [~] | Dev | P2 |
| T8.7 | Red  | • **Achieve:** Pin ingest permission gate behavior + `create_user` always recorded.<br>• **Deliver:** `tests/unit/test_ingest_permission.py` — with `RAGENT_PERMISSION_INGEST_ENABLED=true`, `GET /ingest/{id}` and `DELETE /ingest/{id}` call `batch_check([id])` and 403 problem+json when not allowed; `GET /ingest` filters via `list_objects` (or `batch_check` fallback). Flag `false` (default) leaves all three endpoints unrestricted. `documents.create_user` is set from `preferred_username` on POST regardless of flag. | T8.4 | [~] | QA | P2 |
| T8.8 | Green | • **Achieve:** Wire opt-in ingest permission gate at three call sites.<br>• **Deliver:** `services/ingest_service.py::get/delete`; `list` calls `list_objects` then `WHERE document_id IN (...)`, falling back to per-page `batch_check` when `list_objects` returns `None`. All no-op unless `RAGENT_PERMISSION_INGEST_ENABLED=true`. | T8.7 | [~] | Dev | P2 |
| T8.9 | Behavioral | • **Achieve:** Resolve employee identity + write OpenFGA tuples on ingest.<br>• **Deliver:** `HRClient` + JWT `preferred_username` → employee resolution; OpenFGA tuple-write on ingest (`user:<preferred_username>` viewer of `document:<doc_id>`). | T8.2, T8.4 | [~] | Dev | P2 |

---

## Definition of Done — Phase 1

- [ ] Every `[ ]` row in tracks T0–T7 is `[x]`.
- [ ] T8 rows remain `[~]` (auth disabled by design); spec §3.5 and §4.5 still describe the P2 contract.
- [ ] `uv run ruff format . && uv run ruff check . && uv run pytest` exits 0.
- [ ] **TDD test coverage > 92%** — `uv run pytest --cov=src/ragent --cov-fail-under=92` passes; CI rejects PRs that drop below the floor. Coverage is line + branch.
- [ ] Acceptance metrics T7.2 / T7.3 / T7.4 met.
- [ ] Startup guard (T7.5) verified (`pytest tests/unit/test_bootstrap_startup_guard.py`).
- [ ] Every BDD scenario in `00_spec.md` §3.X has a corresponding plan row whose test path matches.
- [ ] `00_journal.md` carries at least one P1 lesson per domain encountered.

---

## Track TA — aiomysql Adoption (async DB layer) — 2026-05-06

> Decision doc: `docs/team/2026_05_06_aiomysql_adoption.md`
> Goal: replace blocking `pymysql` + sync engine with `aiomysql` + `AsyncEngine` throughout the FastAPI/TaskIQ async path.

| # | Category | Task | Status | Owner |
|---|---|---|:---:|---|
| TA.1 | Red | • **Achieve:** Pin async contract for DocumentRepository and ChunkRepository.<br>• **Deliver:** `tests/unit/test_async_repository_contract.py` — asserts all public methods are coroutine functions (`asyncio.iscoroutinefunction`); `AsyncMock` engine; `await repo.create/get/update_status/delete/list/...` all pass. | [x] | QA |
| TA.2 | Green | • **Achieve:** Convert both repos to `async def` with SQLAlchemy `AsyncEngine`.<br>• **Deliver:** `src/ragent/repositories/document_repository.py` + `chunk_repository.py` — `async with self._engine.begin() as conn: await conn.execute(...)`. | [x] | Dev |
| TA.3 | Red | • **Achieve:** Pin async IngestService contract.<br>• **Deliver:** Updated `tests/unit/test_ingest_service_create.py`, `test_ingest_service_delete.py`, `test_ingest_service_list.py` — all service methods `async def`; `await svc.create/get/delete/list/supersede`. | [x] | QA |
| TA.4 | Green | • **Achieve:** Convert IngestService to async.<br>• **Deliver:** `src/ragent/services/ingest_service.py` — all methods `async def`; `await self._repo.*` and `await self._chunks.*`. | [x] | Dev |
| TA.5 | Red | • **Achieve:** Pin router drops `run_in_threadpool`.<br>• **Deliver:** Updated `tests/unit/test_ingest_router.py` — routes call `await svc.method()` without thread dispatch. | [x] | QA |
| TA.6 | Green | • **Achieve:** Simplify ingest router to direct `await` calls.<br>• **Deliver:** `src/ragent/routers/ingest.py` — remove `run_in_threadpool`; routes are pure `async def` with direct `await svc.*`. | [x] | Dev |
| TA.7 | Red | • **Achieve:** Pin reconciler fully-async contract.<br>• **Deliver:** Updated reconciler tests — all `_mark_failed`, `_redispatch_pending`, `_redispatch_uploaded`, `_resume_deleting`, `_repair_multi_ready` are `async def`; `AsyncMock` repos. | [x] | QA |
| TA.8 | Green | • **Achieve:** Convert Reconciler to fully async.<br>• **Deliver:** `src/ragent/reconciler.py` — all methods `async def`; `await self._repo.*` and `await self._chunks.*`. | [x] | Dev |
| TA.9 | Red | • **Achieve:** Pin ingest worker direct `await` on repos (no `to_thread.run_sync` for repo calls).<br>• **Deliver:** Updated `tests/integration/test_worker_minio_cleanup.py` + `test_worker_acquire_nowait.py` with async repo mocks. | [x] | QA |
| TA.10 | Green | • **Achieve:** Refactor ingest worker to `await` repos directly + pipeline bridge via `anyio.from_thread.run`.<br>• **Deliver:** `src/ragent/workers/ingest.py` — async task `await repo.*`; pipeline Haystack components use `anyio.from_thread.run(async_fn, *args)` bridge; heartbeat uses `asyncio.new_event_loop()` per thread. | [x] | Dev |
| TA.11 | Green | • **Achieve:** Wire async engine in composition root + native async health probe.<br>• **Deliver:** `src/ragent/bootstrap/composition.py` — `create_async_engine(aiomysql_dsn)`; `src/ragent/routers/health_probes.py` — `probe_mariadb` uses `async with engine.connect()`; `src/ragent/bootstrap/init_schema.py` — retains sync `pymysql` engine; `pyproject.toml` adds `aiomysql>=0.2.0` + `pytest-asyncio>=0.24` dev dep. | [x] | Dev |
| TA.12 | Refactor | • **Achieve:** Green tests stay green after structural cleanup.<br>• **Deliver:** `uv run ruff format . && uv run ruff check . --fix && uv run pytest --ignore=tests/e2e` all green; `test_async_repository_contract.py` and updated test files pass. | [x] | Reviewer |

---

## Phase 2 — Production Quality (+3 weeks) — *not started*

| # | Category | Task | Status | Owner |
|---|---|---|:---:|---|
| P2.1 | Stability | • **Achieve:** Production-grade HA + observability.<br>• **Deliver:** SRE HA verification report, monitoring dashboards, alerting rules. | [ ] | SRE |
| P2.2 | Security | • **Achieve:** Activate JWT + Permission layer per Track T8; B14 invariant (ES carries no auth fields).<br>• **Deliver:** All `[~]` rows in T8 → `[ ]` → `[x]`; remove `RAGENT_AUTH_DISABLED`; introduce `RAGENT_TRUST_X_USER_ID_HEADER` (default `false`) and per-surface `RAGENT_PERMISSION_INGEST_ENABLED` / `RAGENT_PERMISSION_CHAT_ENABLED` (both default `false`) — wiring lands but enforcement stays opt-in. | [ ] | Dev |
| P2.3 | Behavioral | • **Achieve:** Improve chat ranking via reranker.<br>• **Deliver:** `RerankClient` wired into chat pipeline as `HybridRetrieverWithRerank` SuperComponent. | [ ] | Dev |
| P2.4 | Behavioral | • **Achieve:** Route translate/summarize intents to direct LLM, bypassing retrieval.<br>• **Deliver:** `ConditionalRouter` intent split. | [ ] | Dev |
| P2.5 | Behavioral | • **Achieve:** Replace P1 501 stub with real MCP tool handler.<br>• **Deliver:** MCP Tool real handler in `routers/mcp.py`. | [ ] | Dev |
| P2.6 | Quality | • **Achieve:** Continuous answer-quality + load resilience evidence.<br>• **Deliver:** RAGAS eval in CI; large-file streaming; chaos drills. | [ ] | QA |
| P2.7 | Behavioral | • **Achieve:** Concurrent component execution for ingest/chat.<br>• **Deliver:** Switch ingest/chat to Haystack `AsyncPipeline`. | [ ] | Dev |
| P2.8 | Closure | • **Achieve:** Close P2 with synced docs and lessons.<br>• **Deliver:** Updated `00_spec.md` / `00_plan.md` + new entries in `00_journal.md`. | [ ] | Master |
| P2.9 | Stability | • **Achieve:** Bound MinIO orphan exposure to 24h.<br>• **Deliver:** Orphan MinIO sweeper with TTL 24h on staging objects (`event=minio.orphan_object` audit). | [ ] | SRE |

## Phase 3 — Graph Enhancement (conditional, +4–6 weeks) — *gated*

| # | Category | Task | Status | Owner |
|---|---|---|:---:|---|
| P3.1 | Decision | • **Achieve:** Lock graph DB choice with a written rationale.<br>• **Deliver:** ADR for Graph DB selection (Neo4j Community / ArcadeDB / Memgraph). | [ ] | Architect |
| P3.2 | Behavioral | • **Achieve:** Replace stub with a real graph extractor on the same Protocol.<br>• **Deliver:** `GraphExtractor` implementation replacing `StubGraphExtractor`. | [ ] | Dev |
| P3.3 | Behavioral | • **Achieve:** Add graph retrieval branch to chat pipeline.<br>• **Deliver:** `HybridRetrieverWithGraph` SuperComponent + `LightRAGRetriever` (200 ms TO → []). | [ ] | Dev |
| P3.4 | Governance | • **Achieve:** Govern entity lifecycle in the graph store.<br>• **Deliver:** Entity soft-delete + ref_count + GC + reconciliation cron. | [ ] | Dev |
| P3.5 | Gate | • **Achieve:** Confirm graph track is justified before spend.<br>• **Deliver:** Gate decision: P2 stable ≥ 4 weeks AND hybrid alone underperforms on relational queries. | [ ] | PM |

---

## Phase 1 v2 — Ingest API breaking-change refactor (2026-05-06)

> Source of truth: `docs/team/2026_05_06_ingest_api_v2.md` (6/6 PASS).
> Supersedes the v1 ingest stack (multipart upload, lang-aware chunker,
> chunks DB table, single-MinIO env). Six TIDY-FIRST commits C1–C6, each
> independently `make check` green.

### Tasks superseded by v2

The following v1 tasks are **superseded** (their `[x]` reflects v1 history;
v2 replaces the underlying behavior in C2–C6):

- T0.8 `chunks` DB table → **dropped**, ES is sole chunk store.
- T2.3 / T2.4 `ChunkRepository` → **deleted** in C6.
- T2.7 / T2.8 ingest-create multipart contract → **rewritten** by T2v.22–T2v.27 (C3).
- T2.13 / T2.14 multipart router → **replaced** by T2v.24–T2v.25 (C3).
- T3.1 / T3.2 lang-routed pipeline; T3.2k / T3.2l CSV row-merger → **replaced** by T2v.30–T2v.37 (C4).

### v2 task block (Track T2v)

| # | Category | Task | Commit | Status | Owner |
|---|---|---|:-:|:-:|---|
| T2v.20 | Structural | • **Achieve:** Add v2 documents columns + drop chunks table.<br>• **Deliver:** Alembic revision adding `ingest_type ENUM('inline','file')`, `minio_site VARCHAR(64) NULL`, `source_url VARCHAR(2048) NULL`; **DROP TABLE chunks**; schema-drift test green; `migrations/schema.sql` updated. | C2 | [x] | Dev |
| T2v.21 | Structural | • **Achieve:** Add ES `raw_content` field to `chunks_v1`.<br>• **Deliver:** `resources/es/chunks_v1.json` adds `"raw_content": {"type":"text","index":false,"doc_values":false}`; spec §5.2 sync; ES drift test green. | C2 | [x] | Dev |
| T2v.22 | Red  | • **Achieve:** Pin v2 request schema (discriminated union + validators).<br>• **Deliver:** `tests/unit/test_ingest_request_schema_v2.py` — `ingest_type` discriminator; `content_type ∈ {text/plain, text/markdown, text/html}` (415 outside set); `inline.content` non-empty UTF-8 string ≤ `INGEST_INLINE_MAX_BYTES`; `file.minio_site` validated against runtime registry; `source_url` opaque ≤ 2048; missing required → 422 with `errors[]`. | C3 | [x] | QA |
| T2v.23 | Green | • **Achieve:** Implement Pydantic discriminated request models.<br>• **Deliver:** `src/ragent/schemas/ingest.py` — `IngestMime` enum, `InlineIngestRequest`, `FileIngestRequest`, discriminated union with `discriminator="ingest_type"`. | C3 | [x] | Dev |
| T2v.24 | Red  | • **Achieve:** Pin v2 router contract (JSON only, no multipart).<br>• **Deliver:** `tests/unit/test_ingest_router_v2.py` — 202 happy path inline + file; 415 bad MIME; 413 inline overflow; 422 shape; old multipart `multipart/form-data` → 415. | C3 | [x] | QA |
| T2v.25 | Green | • **Achieve:** Implement v2 router (JSON-only).<br>• **Deliver:** `src/ragent/routers/ingest.py` — replace `UploadFile` handler with `IngestRequest` body; remove `python-multipart` import. **Breaking change.** | C3 | [x] | Dev |
| T2v.26 | Red  | • **Achieve:** Pin service `create` branching contract.<br>• **Deliver:** `tests/unit/test_ingest_service_v2.py` — inline → stage to `__default__` MinIO + `documents.ingest_type='inline'`; file → HEAD-probe caller's `(minio_site, object_key)`, no copy, `documents.ingest_type='file', minio_site=...`; HEAD miss → 422 `INGEST_OBJECT_NOT_FOUND`; readonly site refuses post-READY delete. | C3 | [x] | QA |
| T2v.27 | Green | • **Achieve:** Implement branched create + structured business log.<br>• **Deliver:** `src/ragent/services/ingest_service.py::create` — discriminated dispatch; emit `event=ingest.received {document_id, ingest_type, content_type, source_id, source_app}`. | C3 | [x] | Dev |
| T2v.28 | Red  | • **Achieve:** Pin `MinioSiteRegistry` semantics.<br>• **Deliver:** `tests/unit/test_minio_site_registry.py` — boot fails fast on missing `__default__`; per-site client cached; `get(name)` raises `UnknownMinioSite` on miss; `read_only` flag exposed; HEAD-probe helper (`stat_object`) returns size. | C3 | [x] | QA |
| T2v.29 | Green | • **Achieve:** Implement registry + composition wiring.<br>• **Deliver:** `src/ragent/storage/minio_registry.py` (parses `MINIO_SITES` JSON env) + `bootstrap/composition.py` adds `MinioSiteRegistry` singleton. | C3 | [x] | Dev |
| T2v.30 | Red  | • **Achieve:** Pin `_TextLoader` Haystack component.<br>• **Deliver:** `tests/unit/test_text_loader.py` — accepts inline `str` or worker-fetched `bytes`+`mime_type`; returns `Document(content=str, meta={mime_type, document_id, source_url, source_title, source_app, source_workspace})`. | C4 | [x] | QA |
| T2v.31 | Green | • **Achieve:** Implement `_TextLoader` (~10 LOC).<br>• **Deliver:** `src/ragent/pipelines/factory.py::_TextLoader`. | C4 | [x] | Dev |
| T2v.32 | Red  | • **Achieve:** Pin `_MarkdownASTSplitter` (mistletoe).<br>• **Deliver:** `tests/unit/test_markdown_ast_splitter.py` — atoms: heading-section, fenced code, list, table, blockquote, paragraph; **never split inside** a fenced code block; `meta["raw_content"]` round-trips byte-for-byte; deterministic across re-runs. | C4 | [x] | QA |
| T2v.33 | Green | • **Achieve:** Implement `_MarkdownASTSplitter` via mistletoe AST walk.<br>• **Deliver:** `src/ragent/pipelines/factory.py::_MarkdownASTSplitter`. | C4 | [x] | Dev |
| T2v.34 | Red  | • **Achieve:** Pin `_HtmlASTSplitter` (selectolax).<br>• **Deliver:** `tests/unit/test_html_ast_splitter.py` — drops `<script>/<style>/<nav>/<aside>/<footer>/<header>` (when not nested in `<article>/<main>`); atoms: heading-section, `<pre>`, `<table>`, `<article>` paragraphs; `meta["raw_content"]` is well-formed serialized HTML fragment; deterministic. | C4 | [x] | QA |
| T2v.35 | Green | • **Achieve:** Implement `_HtmlASTSplitter` via selectolax DOM walk.<br>• **Deliver:** `src/ragent/pipelines/factory.py::_HtmlASTSplitter`. | C4 | [x] | Dev |
| T2v.36 | Red  | • **Achieve:** Pin `_BudgetChunker` (mime-agnostic, 1000/1500/100).<br>• **Deliver:** `tests/unit/test_budget_chunker.py` — greedy-pack atoms ≤ `CHUNK_TARGET_CHARS` (1000); atoms > `CHUNK_MAX_CHARS` (1500) hard-split with `CHUNK_OVERLAP_CHARS` (100); concatenated `meta["raw_content"]` matches union of source atom raws. | C4 | [x] | QA |
| T2v.37 | Green | • **Achieve:** Implement `_BudgetChunker`.<br>• **Deliver:** `src/ragent/pipelines/factory.py::_BudgetChunker` (replaces `_CharBudgetChunker`). | C4 | [x] | Dev |
| T2v.38 | Red  | • **Achieve:** Pin `FileTypeRouter` wiring + unroutable failure path.<br>• **Deliver:** `tests/unit/test_pipeline_routing_v2.py` — each MIME routes to its splitter; unknown MIME → `_RaiseUnroutable` → worker FAILED with `error_code=PIPELINE_UNROUTABLE` (mirrors PIPELINE_TIMEOUT). | C4 | [x] | QA |
| T2v.39 | Green | • **Achieve:** Wire pipeline graph: `_TextLoader → FileTypeRouter → {DocumentSplitter \| _MarkdownASTSplitter \| _HtmlASTSplitter} → DocumentJoiner → _IdempotencyClean → _BudgetChunker → DocumentEmbedder → DocumentWriter(ES only)`. **DocumentWriter writes to ES only — no chunks DB.** | C4 | [x] | Dev |
| T2v.40 | Red  | • **Achieve:** Pin chat read-path uses `raw_content` with `content` fallback.<br>• **Deliver:** `tests/unit/test_chat_uses_raw_content.py` — LLM-context builder + `_ExcerptTruncator` use `doc.meta.get("raw_content") or doc.content`; reranker still scores on `content`; `sources[].source_url` populated. | C5 | [x] | QA |
| T2v.41 | Green | • **Achieve:** Implement chat read-path + `source_url` in citations.<br>• **Deliver:** `src/ragent/pipelines/chat.py` updates; `routers/retrieve.py` exposes `source_url` in chunk envelope. | C5 | [x] | Dev |
| T2v.42 | Red  | • **Achieve:** Pin per-step business + failure logs (Logging Rule extension).<br>• **Deliver:** `tests/unit/test_pipeline_logging.py` — each pipeline component (`_TextLoader`, splitter, `_BudgetChunker`, embedder, writer) emits `event=ingest.step.{started,ok,failed}` carrying `{document_id, step, mime_type, atoms_in?, chunks_out?, duration_ms, error_code?, error?}`. Failures map to `PIPELINE_UNROUTABLE` / `EMBEDDER_ERROR` / `ES_WRITE_ERROR` / `PIPELINE_TIMEOUT`; happy-path emits `event=ingest.ready` with chunk count. | C5 | [x] | QA |
| T2v.43 | Green | • **Achieve:** Wire structlog per-step events + correlate via OTEL.<br>• **Deliver:** Each component logs via `structlog.get_logger("ragent.ingest")` with `document_id` bound; reuse existing OTEL span context (T7.1b). | C5 | [x] | Dev |
| T2v.44 | Refactor | • **Achieve:** Delete dead v1 code.<br>• **Deliver:** Remove `_CharBudgetChunker` lang branches, CJK constants, `langdetect`/`nltk` deps, `python-multipart` dep, `ChunkRepository`, multipart imports, T3.2l RowMerger, v1 chunker env vars. | C6 | [x] | Dev |
| T2v.45 | Acceptance | • **Achieve:** Golden end-to-end test with wiremock embedding + testcontainers.<br>• **Deliver:** `tests/e2e/test_ingest_v2_golden.py` — fixtures spin up MariaDB + Redis + ES + MinIO via testcontainers; `pytest-httpserver` mocks Embedding API; for each `(ingest_type, mime) ∈ {inline, file} × {text/plain, text/markdown, text/html}` (6 combos): POST → broker → real worker → real pipeline → assert (a) ES `chunks_v1` rows exist with both `content` (normalized) and `raw_content` (original); (b) MariaDB `documents` row reaches `READY` with correct `ingest_type`/`minio_site`/`source_url`/`source_title`; (c) **no `chunks` table reads** (table dropped); (d) per-step structured-log events present in captured stream; (e) markdown fenced code survives in `raw_content`, HTML `<script>/<nav>` excluded from both fields. | C6 | [x] | QA |

### Source-id review follow-up (Track T-SR, branch `claude/review-source-id-handling-dq6nj`)

| # | Category | Task | Commit | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-SR.1 | Behavioral | • **Achieve:** Cascade ES chunk delete when supersede picks a loser (spec §3.1 was leaking orphan chunks).<br>• **Deliver:** `src/ragent/services/ingest_service.py::supersede` routes losers through `self.delete()` so `claim_for_deletion → fan_out_delete → repo.delete` runs end-to-end; `tests/integration/test_supersede_task.py` asserts `broker.fan_out_delete` once per loser. | 13a2e28 | [x] | Dev |
| T-SR.2 | Structural | • **Achieve:** Capture the surrogate-PK + biz-UNIQUE rule and lock the revision-model decisions.<br>• **Deliver:** New `Database Practices > Mandatory Surrogate PK + Business Unique Key` rule in `docs/00_rule.md`; `docs/team/2026_05_07_revision_model_proposal.md`; spec §7 rows B32 / B33 / B34. | 934a478 / pending | [x] | Architect |
| T-SR.3 | Structural | • **Achieve:** Rename `documents.source_workspace` → `source_meta` and widen to `VARCHAR(1024)` (free-format metadata).<br>• **Deliver:** Migration `migrations/005_rename_source_workspace_to_source_meta.sql` (`ALGORITHM=COPY, LOCK=SHARED`); `migrations/schema.sql`, `resources/es/chunks_v1.json` (with `ignore_above: 1024`); end-to-end rename in `src/ragent/{schemas,repositories,services,workers,plugins,pipelines,routers}` + all tests; `SOURCE_META_MAX = 1024` in `schemas/ingest.py` is the single source for the cap (imported by `schemas/chat.py`, `routers/retrieve.py`); spec §7 row B35; spec §3.1/§3.4/§5.1/§5.2 prose; `docs/API.md`. | pending | [x] | Dev |
| T-SR.4 | Behavioral | • **Achieve:** DB-side survivor election in `pop_oldest_loser_for_supersede` so an out-of-order finish can never delete a strictly-newer row.<br>• **Deliver:** `src/ragent/repositories/document_repository.py` SQL JOIN-on-newest (inner subquery picks `ORDER BY created_at DESC LIMIT 1`; outer pop runs only when the caller's `survivor_id` matches that elected row); `tests/unit/test_document_repository.py::test_pop_oldest_loser_query_elects_survivor_via_db_max_created_at`. | pending | [x] | Dev |
| T-SR.5 | Behavioral | • **Achieve:** Hydration surfaces only `READY` rows so chat never cites mid-flight or DELETING docs.<br>• **Deliver:** `src/ragent/repositories/document_repository.py::get_sources_by_document_ids` adds `AND status = 'READY'`; `tests/unit/test_document_repository.py::test_get_sources_by_document_ids_filters_to_status_ready`. | pending | [x] | Dev |
| T-SR.6 | Structural | • **Achieve:** Auto-create configured MinIO bucket(s) at boot so `POST /ingest` does not 500 on a fresh install.<br>• **Deliver:** `src/ragent/bootstrap/init_schema.py::init_minio_buckets` wired into `auto_init`; idempotent `bucket_exists` → `make_bucket`; `tests/unit/test_init_schema.py` covers create / skip-when-exists / unconfigured-skip / propagated-error branches; `tests/unit/test_minio_site_registry.py` adds coverage for `delete_object` read-only + `NoSuchKey` swallow + non-404 propagation paths. | pending | [x] | Dev |
| T-SR.7 | Structural | • **Achieve:** Split test tiers so the pre-commit gate runs unit + integration only (e2e is its own tier — slow, container-orchestration heavy, flake-prone, run as a release step).<br>• **Deliver:** `Makefile` exposes `make test-gate` (excludes `tests/e2e`) for the pre-commit gate; `make test` continues to run the full suite. | 2de1408 (main) | [x] | Dev |

### ICU analyzer convergence (Track T-ICU, branch `claude/investigate-haystack-icu-analysis-bCaGr`)

| # | Category | Task | Commit | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-ICU.1 | Structural | • **Achieve:** Reconcile spec §5.2 with B26 (prose, JSON block, Decision Log B42).<br>• **Deliver:** spec §5.2 prose (BM25 analyzer paragraph rewritten to ICU + test override), §5.2 JSON block (adds `analysis.analyzer.icu_text` and `analyzer: icu_text` on `text`/`title`), §7 row B42 (test override rationale + alternatives). | eb7480a | [x] | Dev |
| T-ICU.2 | Red | • **Achieve:** Pin ICU analyzer in prod mapping; pin standard analyzer in test mapping; pin `init_es` env override; pin test↔prod structural equality (minus ICU deltas).<br>• **Deliver:** `tests/unit/test_init_schema.py` adds: (a) prod `resources/es/chunks_v1.json` includes `icu_text` analyzer + `text`/`title` reference it; (b) `tests/resources/es/chunks_v1.json` exists, no `analysis` block, `text`/`title` carry no analyzer (default standard); (c) test↔prod equal after stripping ICU deltas; (d) `init_es` reads `RAGENT_ES_RESOURCES_DIR` env when set; falls back to `_ES_RESOURCES`. | 1cc791d | [x] | QA |
| T-ICU.3 | Green | • **Achieve:** Implement env-driven mapping dir + commit two mapping files.<br>• **Deliver:** `resources/es/chunks_v1.json` (ICU); `tests/resources/es/chunks_v1.json` (standard); `src/ragent/bootstrap/init_schema.py::init_es` reads `RAGENT_ES_RESOURCES_DIR`; `tests/conftest.py` sets the env at session scope to point at the test resources dir. | 1cc791d | [x] | Dev |
| T-ICU.4 | Acceptance | • **Achieve:** Manual / staging smoke test for CJK BM25 (S36 coverage gap from B42).<br>• **Deliver:** Documented procedure: operator runs `Dockerfile.es-test` ES, applies prod mapping, indexes a `"產品規格"` doc, verifies `_analyze` tokenises into `["產品", "規格"]` and BM25 query recalls. Tracked as a release-gate manual step; not blocking pre-commit. | T-ICU.3 | [ ] | Ops |

### Reconciler-as-safety-net follow-up (Track T-RR, branch `claude/add-app-doctor-wPAZH`) — 2026-05-08

> Source: in-session audit `2026-05-08`. Five-fix track (F1–F5) decoupling
> retrieval correctness from background cleanup completeness so the
> reconciler is **only** a safety net for crash recovery + disk reclaim,
> not load-bearing for user-visible correctness.

| # | Category | Task | Spec | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-RR.1 | Red | • **Achieve:** Pin `_SourceHydrator` drop-on-miss semantics (B36 / S6j).<br>• **Deliver:** `tests/unit/test_source_hydrator_drop_on_miss.py` — chunk whose `document_id` is absent from `get_sources_by_document_ids` result is **not** in `result["documents"]`; chunk whose `document_id` is present is enriched and kept; mixed batch → only matched docs survive. | B36 | [x] | QA |
| T-RR.2 | Green | • **Achieve:** Implement drop-on-miss in hydrator.<br>• **Deliver:** `src/ragent/pipelines/chat.py::_SourceHydrator.run` — replace pass-through `else: result.append(doc)` with skip; existing match branch unchanged. | B36 | [x] | Dev |
| T-RR.3 | Refactor | • **Achieve:** Update existing chat-pipeline tests that conflated retrieval verification with pass-through hydration.<br>• **Deliver:** `tests/integration/test_chat_pipeline_retrieval.py` — tests asserting "retrieval recalls a doc" must mock `doc_repo.get_sources_by_document_ids` to include the doc; pipeline-level "hydrator passes through on miss" assertions removed (contract changed by B36). | B36 | [x] | Dev |
| T-RR.4 | Red | • **Achieve:** Pin composition no longer requires legacy `MINIO_ENDPOINT` / `MINIO_ACCESS_KEY` / `MINIO_SECRET_KEY` when `MINIO_SITES` is set.<br>• **Deliver:** `tests/unit/test_composition_minio_wiring.py` — with `MINIO_SITES` JSON set and legacy three vars unset, `build_container()` succeeds; `/readyz` minio probe uses `minio_registry.default().client`. | B37 | [x] | QA |
| T-RR.5 | Structural | • **Achieve:** Switch `/readyz` minio probe source from legacy `MinIOClient` to registry default site.<br>• **Deliver:** `src/ragent/bootstrap/app.py::_build_probes` reads `container.minio_registry.default().client`. | B37 | [x] | Dev |
| T-RR.6 | Green | • **Achieve:** Remove unconditional `_require` of legacy MinIO vars when `MINIO_SITES` is set.<br>• **Deliver:** `src/ragent/bootstrap/composition.py` — guard the legacy `MinIOClient` construction (or drop it entirely if no consumer remains). | B37 | [x] | Dev |
| T-RR.7 | Red | • **Achieve:** Pin AI token boot-time pre-warm — wrong `AI_API_AUTH_URL` or stale J1 fails boot, not first request.<br>• **Deliver:** `tests/unit/test_check_infra_ready_token_warm.py` — `_check_infra_ready` invokes `tm.get_token()` for each token manager; failure raises `RuntimeError`. | B38 | [x] | QA |
| T-RR.8 | Green | • **Achieve:** Pre-warm tokens in lifespan startup.<br>• **Deliver:** `src/ragent/bootstrap/app.py::_check_infra_ready` — for each `tm` in `container.token_managers`, run `await anyio.to_thread.run_sync(tm.get_token)` so token-exchange failure surfaces before `/readyz` succeeds. | B38 | [x] | Dev |
| T-RR.9 | Red | • **Achieve:** Pin worker's atomic promote-and-demote on READY (B39 / supersede race window closure).<br>• **Deliver:** `tests/integration/test_worker_atomic_promote.py` — worker finishes doc B → in same tx, B becomes READY and any other `(source_id, source_app)` row with `status='READY'` becomes `DELETING`; combined with B36 hydrator drop, `/chat` retrieves only B's chunks immediately. | B39 | [x] | QA |
| T-RR.10 | Green | • **Achieve:** Implement atomic promote-demote in repository.<br>• **Deliver:** `src/ragent/repositories/document_repository.py::promote_to_ready_and_demote_siblings(document_id, source_id, source_app)` — single tx: `UPDATE … SET status='READY' WHERE document_id=:id AND status='PENDING'` then `UPDATE … SET status='DELETING' WHERE source_id=:src AND source_app=:app AND document_id != :id AND status='READY'`. Worker's READY transition switches to this method. | B39 | [x] | Dev |
| T-RR.11 | Red | • **Achieve:** Pin HTTP `DELETE /ingest/{id}` actually runs `fan_out_delete` (closes B3 cascade gap).<br>• **Deliver:** `tests/integration/test_http_delete_runs_fan_out.py` — DELETE on a READY doc → `PluginRegistry.fan_out_delete(document_id)` invoked once → ES chunks for that doc absent post-DELETE; row hard-deleted; 204. | B40 | [x] | QA |
| T-RR.12 | Structural | • **Achieve:** Inject `PluginRegistry` into `IngestService` so HTTP DELETE can call `fan_out_delete`.<br>• **Deliver:** `src/ragent/services/ingest_service.py` constructor takes a `registry` arg; `bootstrap/app.py::create_app` wires `container.registry` into `IngestService(...)`. | B40 | [x] | Dev |
| T-RR.13 | Green | • **Achieve:** Replace `_has_fan_out` introspection with explicit registry call.<br>• **Deliver:** `src/ragent/services/ingest_service.py::delete` — `await registry.fan_out_delete(document_id)` between status-DELETING and row-delete; remove `_has_fan_out` flag and broker introspection. | B40 | [x] | Dev |
| T-RR.14 | Red | • **Achieve:** Pin worker promote is DB-arbitrated by `MAX(created_at)` so out-of-order worker completion never leaves an older revision READY. Closes the residual race that previously left the reconciler load-bearing for retrieval correctness.<br>• **Deliver:** `tests/integration/test_worker_atomic_promote.py::test_older_worker_finishing_after_newer_pending_self_demotes` and `::test_older_worker_finishing_after_newer_ready_self_demotes`. | B41 | [x] | QA |
| T-RR.15 | Green | • **Achieve:** Implement DB-side survivor election in worker promote; non-survivors self-demote so the newer worker's tx is the one that flips retrieval.<br>• **Deliver:** `src/ragent/repositories/document_repository.py::promote_to_ready_and_demote_siblings` — `SELECT … FROM documents WHERE source_id/app, status IN (PENDING,READY) ORDER BY created_at DESC LIMIT 1 FOR UPDATE` elects survivor; survivor branch promotes + demotes prior READY, non-survivor branch flips own PENDING → DELETING. | B41 | [x] | Dev |
| T-RR.16 | Red | • **Achieve:** Pin that post-READY enrichment (`fan_out`) does NOT run when the worker self-demotes, so READY-only plugins (event publish, cache warm) never see a non-READY doc.<br>• **Deliver:** `tests/integration/test_worker_minio_cleanup.py::test_self_demote_skips_post_ready_fan_out`. | B41 | [x] | QA |
| T-RR.17 | Green | • **Achieve:** Gate worker `fan_out` on promote outcome.<br>• **Deliver:** `promote_to_ready_and_demote_siblings` returns `bool` (True survivor / False self-demoted); `src/ragent/workers/ingest.py` reads return value and skips `container.registry.fan_out(document_id)` when False. Existing test mocks updated to return `True`. Spec row B41 added in `docs/00_spec.md`. | B41 | [x] | Dev |
| T-RR.18 | Red | • **Achieve:** Pin the `FOR UPDATE` lock semantic — a second concurrent promote on the same `(source_id, source_app)` must block on the row lock until the first tx commits, so retrieval correctness is enforced by the DB, not by application-level serialization.<br>• **Deliver:** `tests/integration/test_worker_atomic_promote.py::test_for_update_serializes_concurrent_promotes` — holds the survivor `SELECT … FOR UPDATE` from outside the repo, asserts a parallel promote is blocked via `wait_for(shield(task), timeout=0.1)` raising `TimeoutError`, then releases and verifies the elected survivor wins. | B41 | [x] | QA |


### Retrieve + Ingest API enhancements (branch `claude/add-ingest-filters-EIXOq`) — 2026-05-11

> Source: user request 2026-05-11. Three paired behavioural additions to the retrieve and ingest surfaces.

| # | Category | Task | Spec | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-EF.1 | Behavioral | • **Achieve:** Add `top_k`/`min_score` request params to `POST /retrieve`; expose `source_meta` in chunk response; add Pydantic `response_model=` to all endpoints.<br>• **Deliver:** `src/ragent/routers/retrieve.py` (`ChunkEntry`, `RetrieveResponse`, `top_k`/`min_score` fields); `src/ragent/pipelines/chat.py` (`source_meta` in `doc_to_source_entry`, `top_k`/`min_score` in `run_retrieval`, `_retriever_params` helper); `src/ragent/routers/ingest.py` (four response models, `response_model=` on all routes); `tests/unit/test_retrieve_router.py`. | B7/B35 | [x] | Dev |
| T-EF.2 | Behavioral | • **Achieve:** Add `source_id`/`source_app` filter params to `GET /ingest` list; change list ordering to newest-first (`document_id DESC`).<br>• **Deliver:** `src/ragent/repositories/document_repository.py` (`list()` adds filter params + DESC order + `< :after` cursor); `src/ragent/services/ingest_service.py` (pass-through); `src/ragent/routers/ingest.py` (query params); `tests/unit/test_document_repository.py` + `test_ingest_service_list.py` + `test_ingest_router_v2.py`. | B7 | [x] | Dev |
| T-EF.3 | Behavioral | • **Achieve:** Fix `min_score` implementation — `score_threshold` is not a valid ES retriever `run()` param; apply as post-retrieval filter instead.<br>• **Deliver:** `src/ragent/pipelines/chat.py` (`_retriever_params` drops `score_threshold`; `run_retrieval` filters by score after `pipeline.run()`); four new tests in `tests/unit/test_retrieve_router.py`. | B7 | [x] | Dev |

### top_k hard cap fix (branch `claude/fix-top-k-cap`) — 2026-05-11

| # | Category | Task | Spec | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-EF.4 | Behavioral | • **Achieve:** Enforce `top_k` as a hard post-pipeline cap in `run_retrieval()` — Haystack's `DocumentJoiner` and `_Reranker` use their init `top_k` and may return more results than the per-request value.<br>• **Deliver:** `src/ragent/pipelines/chat.py` (`docs = docs[:top_k]` after pipeline output); `tests/unit/test_retrieve_router.py` (three new tests: hard cap, cap after min_score, top_k=None returns all). | B7 | [x] | Dev |

### score field in retrieve response (branch `claude/fix-top-k-cap`) — 2026-05-11

| # | Category | Task | Spec | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-EF.5 | Behavioral | • **Achieve:** Expose retrieval score in `POST /retrieve` chunk response.<br>• **Deliver:** `score: float | None` added to `ChunkEntry` in `routers/retrieve.py`; `doc_to_source_entry()` in `pipelines/chat.py` includes `"score": doc.score`; two new tests (score present, score=None). | §3.4.4 | [x] | Dev |

### API path versioning (branch `claude/add-api-versioning-7jm6C`) — 2026-05-11

> Source: user request 2026-05-11. All business API paths must carry a `/v1` version segment.

| # | Category | Task | Spec | Status | Owner |
|---|---|---|:-:|:-:|---|
| T-AV.1 | Behavioral | • **Achieve:** Add `/v1` version segment to all business API paths: `POST/GET/DELETE /ingest/v1[/{id}]`, `POST /chat/v1[/stream]`, `POST /retrieve/v1`, `POST /mcp/v1/tools/rag`.<br>• **Deliver:** `src/ragent/routers/{ingest,chat,retrieve,mcp}.py` — routers gain `prefix="/ingest/v1"` / `/chat/v1` / `/retrieve/v1` / `/mcp/v1`; route decorators drop the resource segment. All tests updated to new paths. Spec §3.4 / §4.1.2 path table updated. | §3.4 | [x] | Dev |

## Binary Document Loaders (DOCX / PPTX)

| # | Category | Task | Spec | Status | Owner |
|---|---|:-:|:-:|:-:|---|
| T-BL.1 | Red | • **Achieve:** Pin `_DocxASTSplitter` atom contract.<br>• **Deliver:** `tests/unit/test_docx_ast_splitter.py` — heading/paragraph/table atoms, blank paragraphs skipped, meta passthrough, empty doc → zero atoms, table raw_content is Markdown. | §4.2 | [x] | QA |
| T-BL.2 | Red | • **Achieve:** Pin `_PptxASTSplitter` atom contract.<br>• **Deliver:** `tests/unit/test_pptx_ast_splitter.py` — one atom per slide, blank slide skipped, slide_number in meta, meta passthrough, empty presentation → zero atoms. | §4.2 | [x] | QA |
| T-BL.3 | Green | • **Achieve:** Implement `_DocxASTSplitter` and `_PptxASTSplitter`.<br>• **Deliver:** `src/ragent/pipelines/factory.py` — both components; `IngestMime.DOCX`/`IngestMime.PPTX` enum values; `BINARY_MIMES` frozenset derived from `IngestMime`; `_TextLoader` accepts `content_bytes`; `_MimeAwareSplitter` routes via `IngestMime` enum constants; worker `BINARY_MIMES` branch skips UTF-8 decode; `python-docx`/`python-pptx` added as deps. | §4.2 | [x] | Dev |
| T-BL.4 | Acceptance | • **Achieve:** `_MimeAwareSplitter` dispatch covers all new routes.<br>• **Deliver:** `tests/unit/test_pipeline_routing_v2.py` — DOCX and PPTX routes exercise the full dispatch path end-to-end. | §4.2 | [x] | QA |

### DOCX/PPTX splitter review fixes (PR #54) — 2026-05-11

| # | Category | Task | Spec | Status | Owner |
|---|---|:-:|:-:|:-:|---|
| T-BL.5 | Structural | • **Achieve:** Address Gemini/Codex PR review findings.<br>• **Deliver:** `_DocxASTSplitter`/`_PptxASTSplitter` strip `raw_bytes` from atom meta (`base_meta`); `_PptxASTSplitter` extracts table cell text; `_table_to_markdown` escapes `|` and normalises newlines in cell text; `_PptxASTSplitter` uses `para.text` instead of manual run join. | §4.2 | [x] | Dev |

### PPTX mime_type UX + pipeline error fix — 2026-05-12

| # | Category | Task | Spec | Status | Owner |
|---|---|:-:|:-:|:-:|---|
| T-BL.6 | Behavioral | • **Achieve:** Accept short aliases `pptx`/`docx` at all API entry points.<br>• **Deliver:** `IngestMime._missing_` classmethod + module-level `_MIME_ALIASES` constant; `BINARY_MIMES` moved to `schemas/ingest.py` as canonical source; `factory.py` and `workers/ingest.py` import from there. Alias coverage: JSON body, Form field (upload endpoint), case-insensitive. | §4.1 | [x] | Dev |
| T-BL.7 | Behavioral | • **Achieve:** Reject binary MIME (DOCX/PPTX) on `ingest_type=inline` at schema validation time.<br>• **Deliver:** `InlineIngestRequest.model_validator(mode="after")` raises 422 `INGEST_VALIDATION` when `mime_type in BINARY_MIMES`; clears "file is not a zip file" pipeline crash for inline callers. | §4.1 | [x] | Dev |
| T-BL.8 | Behavioral | • **Achieve:** Worker uses `doc.mime_type` (DB) as authoritative MIME routing key, not MinIO content-type.<br>• **Deliver:** `workers/ingest.py` — `mime = doc.mime_type or minio_content_type or DEFAULT_MIME`; fixes "file is not a zip file" for `ingest_type=file` when caller's MinIO object has generic/missing content-type. | §4.1 | [x] | Dev |
| T-BL.9 | Behavioral | • **Achieve:** Case-insensitive MIME handling per RFC 2045 §5.1 (PR #58 review).<br>• **Deliver:** `IngestMime._missing_` accepts full MIME strings with mixed casing (e.g., `TEXT/PLAIN`) via lowercase-enum-value fallback after the alias dict; worker lowercases the resolved mime so MinIO metadata with non-standard casing (e.g., `APPLICATION/OCTET-STREAM`) routes correctly. | §4.1 | [x] | Dev |


### Ingest pipeline file-type bug fixes — 2026-05-12

| # | Category | Task | Spec | Status | Owner |
|---|---|:-:|:-:|:-:|---|
| T-FIL.1 | Behavioral | • **Achieve:** Fix `head_object` `or 0` bug that caused `OSError: MinIO get_object size mismatch` for any file whose `stat.size` is `None`.<br>• **Deliver:** `storage/minio_registry.py` — remove `or 0` from `size = getattr(stat, "size", None)`; update return type to `tuple[int \| None, str \| None] \| None`; regression tests in `tests/unit/test_minio_site_registry.py`. | §3.1 | [x] | Dev |
| T-FIL.2 | Behavioral | • **Achieve:** Enforce `INGEST_FILE_MAX_BYTES` limit for `ingest_type=file` ingests (spec §4.2).<br>• **Deliver:** `services/ingest_service.py` — `_record_file` checks `stat_object` size against limit and raises `FileTooLarge`; `create()` accepts `max_file_bytes` parameter; test `test_file_too_large_raises_413` in `tests/unit/test_ingest_service_v2.py`. | §4.2 | [x] | Dev |
| T-FIL.3 | Behavioral | • **Achieve:** Replace `SELECT … FOR UPDATE` with lock-free atomic correlated-subquery UPDATE in `promote_to_ready_and_demote_siblings`.<br>• **Deliver:** `repositories/document_repository.py` — election uses MVCC subquery wrapped in derived table (MariaDB restriction), outer UPDATE locks only the single target row; no blocking between concurrent workers; existing `idx_source_app_id_status_created` index used; docker integration tests updated to verify correct survivor election. | §B39 | [x] | Dev |
| T-FIL.4 | Behavioral | • **Achieve:** Verify `ingest_type=file` worker never calls `delete_object` (caller owns the bytes).<br>• **Deliver:** `tests/integration/test_worker_minio_cleanup.py` — `test_file_type_ingest_does_not_delete_minio_object` added; removed incorrect `pytestmark = pytest.mark.docker` (mock-only tests do not require Docker). | §3.1 | [x] | Dev |
